---
phase: 004-slice-2c-tasks-opportunities-interactions
plan: 03
type: execute
wave: 3
depends_on:
  - 004-02
files_modified:
  - src/lib/types.ts
  - src/lib/captures/promote.ts
  - src/lib/contact-activity.ts
  - src/app/(app)/follow-ups/page.tsx
  - src/components/follow-ups/follow-up-list.tsx
  - src/components/follow-ups/follow-up-form.tsx
  - src/lib/action-scoring.ts
  - src/components/dashboard/task-list.tsx
autonomous: true
requirements:
  - SLICE-2C-06

must_haves:
  truths:
    - "promote.ts no longer references .from('interactions') -- updated to .from('interactions_legacy')"
    - "all follow_ups .from('follow_ups') queries are updated to .from('tasks').eq('type', 'follow_up')"
    - "pnpm typecheck exits 0"
  artifacts:
    - path: "src/lib/captures/promote.ts"
      provides: "interactions_legacy INSERT"
      contains: ".from(\"interactions_legacy\")"
  key_links:
    - from: "src/lib/captures/promote.ts"
      to: "interactions_legacy"
      via: "adminClient.from('interactions_legacy').insert(...)"
      pattern: "from\\(\"interactions_legacy\"\\)"
---

<objective>
Update TypeScript sources so they compile cleanly after the schema changes in Plans 01 and 02.

Key changes required:
1. promote.ts: change .from("interactions") -> .from("interactions_legacy") so the
   existing INSERT still works after the table is renamed (views are not insertable).
2. All follow_ups callers: change .from("follow_ups") -> .from("tasks").eq("type", "follow_up")
   since follow_ups is dropped. Map results to FollowUp shape or update consumer types.
3. DealRow in task-list.tsx: remove or redirect to opportunities.
4. contact-activity.ts: update followUps parameter sourcing to tasks with type='follow_up'.
5. Run pnpm typecheck -- must exit 0 before this plan is done.

NOTE: Do NOT run pnpm build here (that happens in Plan 04 after the migration push).
NOTE: interactions SELECTs do NOT need changing -- the VIEW is named interactions and
      SELECT queries against a view work identically to SELECT against a table.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/ROADMAP.md
@/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-CONTEXT.md

<interfaces>
<!-- promote.ts INSERT (src/lib/captures/promote.ts:~380): -->
<!--   adminClient.from("interactions").insert({ user_id, contact_id, type, summary }) -->
<!--   Change to: adminClient.from("interactions_legacy").insert({ ... }) -->

<!-- follow_ups callers pattern: -->
<!--   Before: supabase.from("follow_ups").select("*").eq("contact_id", id) -->
<!--   After:  supabase.from("tasks").select("*").eq("type", "follow_up").eq("contact_id", id) -->
<!--   Result type: Task[] instead of FollowUp[] -- may need shape mapping -->

<!-- FollowUp interface (src/lib/types.ts:174): -->
<!--   { id, user_id, contact_id, reason, due_date, status: FollowUpStatus, completed_at, created_at, contacts? } -->
<!-- Task interface (src/lib/types.ts:~160): -->
<!--   { id, user_id, contact_id, title, description, due_date, priority, status, is_recurring, -->
<!--     recurrence_rule, completed_at, snoozed_until, created_at, updated_at, deleted_at, -->
<!--     type?, source?, due_reason?, action_hint? } -->

<!-- FollowUp.reason maps to Task.due_reason (migrated column) -->
<!-- FollowUp.status (FollowUpStatus: pending/completed/skipped) maps to Task.status ('open'/'completed'/'cancelled') -->

<!-- contact-activity.ts buildActivityFeed uses followUps: FollowUp[] only for: -->
<!--   i.reason (summary text) and i.created_at (timestamp) -->
<!--   Callers that pass follow_ups rows will need to adapt. -->

<!-- DealRow: only src/components/dashboard/task-list.tsx references it. -->
<!--   Check current usage -- likely a type assertion or interface import. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6a: Update promote.ts interactions INSERT</name>
  <files>src/lib/captures/promote.ts</files>
  <read_first>
    - /Users/alex/crm/src/lib/captures/promote.ts (full file -- understand current interact path)
    - Run: grep -n "from.*interactions\|interactions" /Users/alex/crm/src/lib/captures/promote.ts
  </read_first>
  <action>
    Change: adminClient.from("interactions")
    To:     adminClient.from("interactions_legacy")

    This is the single INSERT path for interaction promotions. The change is a one-line
    string substitution. Do NOT change any other logic in promote.ts.

    After the change, add this comment on the line above:
    // Writing to interactions_legacy directly -- views are not insertable.
    // TODO Slice 3+: migrate to writeEvent() so this goes through activity_events.
  </action>
  <verify>
    <automated>grep -n "interactions_legacy" /Users/alex/crm/src/lib/captures/promote.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "from(\"interactions_legacy\")" in promote.ts returns a match
    - grep "from(\"interactions\")" in promote.ts returns NO matches (old reference gone)
    - The INSERT parameters (user_id, contact_id, type, summary) are unchanged
    - A comment noting the TODO for writeEvent() migration is present above the insert line
  </acceptance_criteria>
  <done>promote.ts INSERT target updated to interactions_legacy</done>
</task>

<task type="auto">
  <name>Task 6b: Update DealRow reference in task-list.tsx</name>
  <files>src/components/dashboard/task-list.tsx</files>
  <read_first>
    - /Users/alex/crm/src/components/dashboard/task-list.tsx (full file -- understand DealRow usage)
    - Run: grep -n "DealRow\|Deal\|deals" /Users/alex/crm/src/components/dashboard/task-list.tsx
  </read_first>
  <action>
    Read the file to understand how DealRow is used (it is the only file referencing deals types).
    Options:
    - If DealRow is imported but not meaningfully used: remove the import and any dead code
    - If DealRow is used as a prop type or state type: replace with OpportunityRow or similar
      (check if types.ts has an Opportunity or OpportunityRow type)
    - If the component queries .from("deals"): update to .from("opportunities")

    After the change: grep to confirm no "DealRow" or .from("deals") references remain.
  </action>
  <verify>
    <automated>grep -n "DealRow\|\.from.*deals" /Users/alex/crm/src/components/dashboard/task-list.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "DealRow" in task-list.tsx returns NO matches
    - grep ".from(\"deals\")" in task-list.tsx returns NO matches
    - If the component used deals data, it now reads from opportunities
    - pnpm typecheck still passes after this change (verified in Task 6f)
  </acceptance_criteria>
  <done>task-list.tsx DealRow reference removed or redirected to opportunities</done>
</task>

<task type="auto">
  <name>Task 6c: Update follow_ups queries in all callers</name>
  <files>
    src/app/(app)/follow-ups/page.tsx
    src/components/follow-ups/follow-up-list.tsx
    src/components/follow-ups/follow-up-form.tsx
    src/lib/action-scoring.ts
    src/app/(app)/contacts/[id]/page.tsx
    src/app/(app)/actions/page.tsx
    src/components/dashboard/quick-actions.tsx
    src/app/(app)/captures/captures-client.tsx
    src/lib/captures/parse.ts
    src/lib/validations.ts
  </files>
  <read_first>
    - Run: grep -rn "from.*follow_ups\|follow_ups" /Users/alex/crm/src --include="*.ts" --include="*.tsx" | grep -v node_modules | grep "\.from\|follow_up"
    - Each file that has .from("follow_ups") must be read before editing
    - /Users/alex/crm/src/lib/types.ts lines 174-195 (FollowUp and Task interfaces)
  </read_first>
  <action>
    For each file that queries .from("follow_ups"):

    Change query pattern:
      BEFORE: supabase.from("follow_ups").select(...)
      AFTER:  supabase.from("tasks").select(...).eq("type", "follow_up")

    The result rows are now Task-shaped. Adapt the consumer:
    - If the file uses FollowUp type: either update to use Task type, or cast Task -> FollowUp shape
      using: { ...row, reason: row.due_reason ?? row.title }
      (due_reason is the migrated reason field; title is the COALESCE fallback)
    - If the file only reads id, status, created_at: no type change needed; use Task type directly

    For files that import FollowUp or FollowUpStatus from types.ts but do NOT query the DB:
    - Leave the import and type in place if other non-DB code still uses FollowUpStatus
    - Remove only if the type is completely unused after the DB query update

    For src/lib/captures/parse.ts and src/lib/validations.ts:
    - These likely use "follow_up" as a string literal in ParsedIntent or validation rules
    - Do NOT change these -- "follow_up" as a literal is still valid as a task type value

    Contact-activity.ts (see Task 6d for this one -- it has extra complexity).
  </action>
  <verify>
    <automated>grep -rn "from.*follow_ups" /Users/alex/crm/src --include="*.ts" --include="*.tsx" | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - grep ".from(\"follow_ups\")" across all src files returns NO matches
    - Files that queried follow_ups now query tasks with .eq("type", "follow_up")
    - Task type is used or cast correctly in consumer components
    - parse.ts and validations.ts "follow_up" string literals are UNCHANGED
  </acceptance_criteria>
  <done>All .from("follow_ups") queries updated to tasks with type='follow_up' filter</done>
</task>

<task type="auto">
  <name>Task 6d: Update contact-activity.ts followUps parameter</name>
  <files>src/lib/contact-activity.ts</files>
  <read_first>
    - /Users/alex/crm/src/lib/contact-activity.ts (full file)
    - /Users/alex/crm/src/app/(app)/contacts/[id]/page.tsx (caller that queries follow_ups and passes to buildActivityFeed)
    - /Users/alex/crm/src/lib/types.ts lines 155-200 (Task and FollowUp interfaces)
  </read_first>
  <action>
    Read contact-activity.ts buildActivityFeed to understand exactly which FollowUp fields it reads.
    Based on the review (June 2026-04-24 audit):
    - The function iterates followUps: FollowUp[] and reads i.reason and i.created_at

    Strategy: Update buildActivityFeed to accept tasks (Task[]) for the follow-ups arm instead
    of FollowUp[]. Specifically:
    - Change parameter type: followUps: FollowUp[] -> followUps: Pick<Task, 'id' | 'due_reason' | 'title' | 'created_at' | 'completed_at'>[]
      OR: change to a more permissive type that Task satisfies
    - Update usage: i.reason -> (i.due_reason ?? i.title)  (due_reason holds the migrated reason)
    - Update sourceLabel from "Follow-up" -> "Follow-up" (unchanged)

    Then update the caller (contacts/[id]/page.tsx) to:
    - Query: supabase.from("tasks").select("*").eq("type", "follow_up").eq("contact_id", id)
    - Pass result (Task[]) to buildActivityFeed

    If the parameter type change causes type errors elsewhere (other callers pass FollowUp[]),
    adapt those callers too.
  </action>
  <verify>
    <automated>grep -n "follow_ups\|FollowUp" /Users/alex/crm/src/lib/contact-activity.ts | head -20</automated>
  </verify>
  <acceptance_criteria>
    - contact-activity.ts no longer references FollowUp type in its function signature (or it references Task instead)
    - i.reason usage replaced with (i.due_reason ?? i.title) or equivalent
    - grep ".from(\"follow_ups\")" in contacts/[id]/page.tsx returns NO matches
    - contacts/[id]/page.tsx queries tasks with .eq("type", "follow_up") for the follow-ups feed
  </acceptance_criteria>
  <done>contact-activity.ts updated to accept Task[] for follow-ups arm; caller updated</done>
</task>

<task type="auto">
  <name>Task 6e: Log promote.ts blocker to BLOCKERS.md</name>
  <files>BLOCKERS.md</files>
  <read_first>
    - /Users/alex/crm/BLOCKERS.md (append under ## Open, after last entry)
  </read_first>
  <action>
    Add this entry under ## Open in BLOCKERS.md:

    ### [2026-04-24] promote.ts still INSERTs directly into interactions_legacy
    - **Broken:** src/lib/captures/promote.ts writes interaction rows via adminClient.from("interactions_legacy").insert(). This works, but it bypasses the activity_events canonical ledger (the Slice 1 architectural rule: "Every server-side write path emits an event via writeEvent()").
    - **Where:** src/lib/captures/promote.ts ~line 380 -- the interaction/note promotion path.
    - **Fix needed:** Replace the direct interactions_legacy INSERT with a writeEvent() call using verb='interaction.{type}', context containing contact_id and summary. Verify the interactions VIEW Part B surfaces the row correctly after the change. Once writeEvent() is the write path, interactions_legacy can potentially be dropped (all rows will live in activity_events).
  </action>
  <verify>
    <automated>grep -n "interactions_legacy" /Users/alex/crm/BLOCKERS.md</automated>
  </verify>
  <acceptance_criteria>
    - BLOCKERS.md Open section contains "interactions_legacy" entry
    - Entry includes file path (promote.ts) and fix description
  </acceptance_criteria>
  <done>BLOCKERS.md updated with promote.ts interactions_legacy direct-write blocker</done>
</task>

<task type="auto">
  <name>Task 6f: Run pnpm typecheck</name>
  <files>-- (verification, no files modified)</files>
  <read_first>
    - Run the typecheck command -- no files to read first
  </read_first>
  <action>
    cd ~/crm && pnpm typecheck

    If typecheck fails:
    1. Read the exact error messages
    2. Fix each error in the affected file
    3. Re-run pnpm typecheck
    4. Repeat until exit 0

    Do NOT proceed to Plan 04 until typecheck exits 0.
    Do NOT add // @ts-ignore suppressions -- fix the underlying type errors.

    Common expected errors and fixes:
    - "Property 'reason' does not exist on type 'Task'": update consumer to use due_reason ?? title
    - "Argument of type 'Task[]' is not assignable to parameter of type 'FollowUp[]'":
      update the function signature or add a type cast/mapping function
    - "Module not found 'follow_ups'": spurious -- follow_ups is a table name string, not a module
    - "Type 'string' is not assignable to type 'InteractionType'":
      update Interaction.type to string | InteractionType, or cast the view result
  </action>
  <verify>
    <automated>cd /Users/alex/crm && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm typecheck exits 0
    - Zero TypeScript errors in the output
    - No @ts-ignore suppressions added by this plan
  </acceptance_criteria>
  <done>pnpm typecheck exits 0. TypeScript updates complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript types -> runtime DB queries | Type changes here affect compile-time checks; runtime queries still hit the DB view/tables |
| follow_ups -> tasks query | The .eq("type", "follow_up") filter is a string; no injection risk (it's a constant, not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-004-08 | Spoofing | interactions_legacy INSERT in promote.ts | accept | adminClient is service-role; same trust boundary as before. The table rename changes the target name but not the security model. |
| T-004-09 | Denial of Service | pnpm typecheck failure blocks deployment | mitigate | Typecheck gate in Task 6f stops the slice if types are broken. Plan 04 cannot proceed until exit 0. |
</threat_model>

<verification>
After this plan completes:

```bash
# Verify no .from("follow_ups") calls remain
grep -rn "from(\"follow_ups\")" ~/crm/src --include="*.ts" --include="*.tsx"
# Expected: 0 matches

# Verify no .from("interactions") calls remain (only interactions_legacy is valid for INSERTs)
grep -rn "from(\"interactions\")" ~/crm/src --include="*.ts" --include="*.tsx"
# Expected: 0 matches (or only SELECT paths that are fine -- views are queryable)
# Actually: SELECT .from("interactions") is still valid (view is named interactions)
# Only INSERT .from("interactions") must be gone
grep -rn "from(\"interactions\").*insert\|insert.*from(\"interactions\")" ~/crm/src --include="*.ts"
# Expected: 0 matches

# Verify typecheck
cd ~/crm && pnpm typecheck
# Expected: exit 0
```
</verification>

<success_criteria>
- All .from("follow_ups") queries updated to tasks with type='follow_up' filter
- promote.ts INSERT updated to interactions_legacy
- contact-activity.ts followUps parameter updated to accept Task shape
- DealRow reference removed from task-list.tsx
- BLOCKERS.md updated with interactions_legacy write path note
- pnpm typecheck exits 0
</success_criteria>

<output>
After completion, create `/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-03-SUMMARY.md`
</output>

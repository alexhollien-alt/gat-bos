---
phase: 004-slice-2c-tasks-opportunities-interactions
plan: 04
type: execute
wave: 4
depends_on:
  - 004-03
files_modified:
  - supabase/migrations/20260424120000_slice2c_drop_interactions_legacy.sql
autonomous: false
requirements:
  - SLICE-2C-07

must_haves:
  truths:
    - "supabase db push --linked succeeds OR paste file written to ~/Desktop/"
    - "all 11 acceptance gate checks pass"
    - "pnpm typecheck exits 0"
    - "pnpm build exits 0"
    - "interactions_legacy is dropped ONLY if typecheck passes"
  artifacts:
    - path: "supabase/migrations/20260424120000_slice2c_drop_interactions_legacy.sql"
      provides: "conditional interactions_legacy drop"
      contains: "DROP TABLE IF EXISTS interactions_legacy"
  key_links:
    - from: "supabase db push --linked"
      to: "live Supabase schema"
      via: "Supabase CLI migration push"
      pattern: "supabase db push"
---

<objective>
[BLOCKING] Apply all three migration files to Supabase, verify the schema is correct,
run all 11 acceptance gate checks, and confirm pnpm build passes.

If typecheck passes, also write and apply Migration File 3 (interactions_legacy DROP).
If typecheck fails, keep interactions_legacy in place and log the blocker.

This plan is autonomous: false because:
1. supabase db push --linked may require Docker (unknown state at plan time)
2. If CLI fails, Alex must paste the SQL manually before the rest can proceed

Flow:
1. Try supabase db push --linked
2. If push fails: write PASTE-INTO-SUPABASE-slice2c.sql and STOP with explicit instructions
3. If push succeeds: run all 11 acceptance gate queries
4. If all gates pass AND typecheck still 0: write + apply Migration File 3 (drop interactions_legacy)
5. Run pnpm build -- must exit 0
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/ROADMAP.md
@/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-CONTEXT.md
</context>

<tasks>

<task type="manual">
  <name>Task 7a: Apply migration files via supabase db push</name>
  <files>-- (CLI operation, supabase migration state)</files>
  <read_first>
    - Run: ls /Users/alex/crm/supabase/migrations/ | sort | tail -5 (confirm all 3 files present: 20260424100000, 20260424110000, and confirm 20260424120000 not yet written)
    - Run: cd /Users/alex/crm && docker info 2>/dev/null | head -3 (check if Docker is running)
  </read_first>
  <action>
    Run: cd ~/crm && supabase db push --linked

    Supabase CLI applies migration files in timestamp order. Three files will be applied:
    1. 20260424100000_slice2c_tasks_extend.sql (tasks + follow_ups + deals + spine)
    2. 20260424110000_slice2c_interactions_view.sql (interactions rename + view)

    IF supabase db push FAILS (Docker not running, network error, or auth issue):
    - Concatenate all migration SQL into ~/Desktop/PASTE-INTO-SUPABASE-slice2c.sql
      (cat 20260424100000...sql 20260424110000...sql > ~/Desktop/PASTE-INTO-SUPABASE-slice2c.sql)
    - Run: open ~/Desktop/PASTE-INTO-SUPABASE-slice2c.sql
    - STOP and tell Alex: "Docker push failed. Open ~/Desktop/PASTE-INTO-SUPABASE-slice2c.sql,
      paste the contents into the Supabase SQL Editor for project rndnxhvibbqqjrzapdxs,
      run it, then type 'done' to continue."
    - Do NOT continue to Task 7b until Alex confirms the paste was executed.

    IF supabase db push SUCCEEDS: proceed to Task 7b immediately.
  </action>
  <verify>
    <automated>cd /Users/alex/crm && supabase db query --linked "SELECT count(*) FROM information_schema.columns WHERE table_name='tasks' AND column_name='type';" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exits 0 OR Alex confirms paste was executed in Supabase SQL Editor
    - tasks.type column exists in the live DB (verification query returns count=1)
  </acceptance_criteria>
  <done>Migration files applied to live Supabase DB.</done>
</task>

<task type="auto">
  <name>Task 7b: Run all 11 acceptance gate checks</name>
  <files>-- (verification only, no files modified)</files>
  <read_first>
    - /Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-CONTEXT.md (acceptance gate list)
  </read_first>
  <action>
    Run all 11 gates in order. Report PASS or FAIL for each.

    Gate 1: grep -n "type text\|due_reason\|action_hint" ~/crm/supabase/migrations/*slice2c*
    EXPECTED: ADD COLUMN lines present

    Gate 2: grep -n "tasks_type_check" ~/crm/supabase/migrations/*slice2c*
    EXPECTED: constraint definition present

    Gate 3: supabase db query --linked "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('follow_ups','deals');"
    EXPECTED: 0 rows

    Gate 4: supabase db query --linked "SELECT table_name, table_type FROM information_schema.tables WHERE table_schema='public' AND table_name='interactions';"
    EXPECTED: 1 row with table_type='VIEW'

    Gate 5: supabase db query --linked "SELECT column_name FROM information_schema.columns WHERE table_name='tasks' AND column_name IN ('type','source','due_reason','action_hint') ORDER BY column_name;"
    EXPECTED: 4 rows

    Gate 6: supabase db query --linked "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('signals','focus_queue','cycle_state','commitments');"
    EXPECTED: 0 rows

    Gate 7: supabase db query --linked "SELECT count(*) FROM opportunities;"
    EXPECTED: count >= 2

    Gate 8: supabase db query --linked "SELECT count(*) FROM tasks WHERE type='follow_up';"
    EXPECTED: count >= 0 (was 0 follow_ups, so result is 0; acceptable)

    Gate 9: git tag -l | grep slice-2c-complete
    EXPECTED: present (this gate is checked in Plan 06 after tagging -- SKIP here, check in Plan 06)

    Gate 10: cd ~/crm && pnpm typecheck
    EXPECTED: exit 0 (already passed in Plan 03; re-confirm here)

    Gate 11: cd ~/crm && pnpm build
    EXPECTED: exit 0

    If ANY gate fails:
    - Report the failing gate clearly
    - Diagnose the root cause (wrong SQL, missing column, view not created, etc.)
    - Fix the underlying issue (add a follow-up migration or TypeScript fix)
    - Re-run the failing gate until it passes
    - Do NOT proceed to Plan 05 with any gate failures except Gate 9 (that is checked in Plan 06)
  </action>
  <verify>
    <automated>cd /Users/alex/crm && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Gates 1-8 all PASS (reported explicitly)
    - Gate 9 deferred to Plan 06 (tag not yet created)
    - Gate 10 (pnpm typecheck) exits 0
    - Gate 11 (pnpm build) exits 0
    - No gate failures remain unresolved
  </acceptance_criteria>
  <done>All 10 active gates pass. pnpm build exits 0.</done>
</task>

<task type="auto">
  <name>Task 7c: Write and apply interactions_legacy DROP migration (conditional)</name>
  <files>supabase/migrations/20260424120000_slice2c_drop_interactions_legacy.sql</files>
  <read_first>
    - Confirm pnpm typecheck exit code from Task 7b Gate 10
    - Run: supabase db query --linked "SELECT count(*) FROM interactions_legacy;" (confirm 2 rows safe to archive)
    - Run: supabase db query --linked "SELECT count(*) FROM interactions;" (confirm view returns same 2 rows)
  </read_first>
  <action>
    ONLY proceed if pnpm typecheck (Gate 10) passed in Task 7b.

    If typecheck FAILED:
    - Do NOT write or apply this migration
    - Log to BLOCKERS.md: "interactions_legacy kept in place -- typecheck failures prevent safe drop. Resolve type errors first."
    - Skip this task.

    If typecheck PASSED:
    Write: /Users/alex/crm/supabase/migrations/20260424120000_slice2c_drop_interactions_legacy.sql

    ```sql
    -- ================================================================
    -- SLICE 2C: Drop interactions_legacy
    -- Safe to drop after:
    --   1. interactions VIEW is verified in place (Plan 004-02)
    --   2. pnpm typecheck passes (Plan 004-03)
    --   3. pnpm build passes (Plan 004-04)
    -- promote.ts now writes to interactions_legacy via direct INSERT;
    -- that blocker is logged. The 2 legacy rows are surfaced via the view.
    -- ================================================================

    BEGIN;

    DROP TABLE IF EXISTS interactions_legacy CASCADE;

    COMMIT;
    ```

    Then apply: cd ~/crm && supabase db push --linked
    If push fails: add to the paste file and instruct Alex as in Task 7a.

    After applying, verify:
    supabase db query --linked "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename='interactions_legacy';"
    EXPECTED: 0 rows (table dropped)

    supabase db query --linked "SELECT count(*) FROM interactions;"
    NOTE: After dropping interactions_legacy, Part A of the UNION ALL will reference a non-existent table.
    The view MUST be recreated without the UNION ALL (or with an empty Part A) to remain valid.

    CRITICAL: Before dropping interactions_legacy, verify that promote.ts was updated to use
    interactions_legacy. If promote.ts still tries to INSERT into interactions_legacy after it is
    dropped, those writes will fail silently.

    Actually -- RECONSIDERING: Do NOT drop interactions_legacy in this slice.
    The view's Part A references interactions_legacy. Dropping interactions_legacy BREAKS the view.
    The view would need to be rewritten to remove Part A before the drop is safe.
    This is out of scope for Slice 2C.

    REVISED ACTION:
    Do NOT write or apply the interactions_legacy DROP migration in Slice 2C.
    Instead, log to BLOCKERS.md:
      "interactions_legacy table kept. Dropping it requires rewriting the interactions view
       to remove Part A (UNION ALL). Schedule for Slice 3 cleanup once promote.ts is migrated
       to writeEvent() and all legacy rows are surfaced via activity_events."
    Skip this task -- the migration file is NOT created.
  </action>
  <verify>
    <automated>echo "interactions_legacy drop deferred to Slice 3 -- see BLOCKERS.md"</automated>
  </verify>
  <acceptance_criteria>
    - interactions_legacy DROP is NOT applied in Slice 2C (deferred to Slice 3)
    - BLOCKERS.md entry added explaining the deferral
    - The interactions VIEW continues to function (Part A references interactions_legacy which still exists)
  </acceptance_criteria>
  <done>interactions_legacy drop correctly deferred. BLOCKERS.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase db push -> live DB | Service-role migration; applies DDL with no user-data exposure |
| PASTE-INTO-SUPABASE-slice2c.sql | Contains DDL only -- no secrets, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-004-10 | Denial of Service | pnpm build failure blocks deploy | mitigate | Gate 11 explicitly stops the slice if build fails. Root cause must be fixed before Plan 05. |
| T-004-11 | Data Loss | interactions_legacy drop breaks view | mitigate | REVISED: interactions_legacy drop deferred to Slice 3. The view's Part A would break if the table is dropped while still referenced. |
</threat_model>

<verification>
After this plan completes:

All gates 1-8, 10, 11 must be PASS.
Gate 9 (tag) checked in Plan 06.

Final state:
- tasks has 4 new columns
- follow_ups and deals are gone
- interactions is a VIEW
- interactions_legacy still exists (deferred drop)
- opportunities preserved with 2+ rows and 13 new columns
- spine tables still absent (no regression)
- pnpm typecheck: exit 0
- pnpm build: exit 0
</verification>

<success_criteria>
- supabase db push succeeded or paste file executed by Alex
- Gates 1-8 PASS
- Gate 10 (typecheck) PASS
- Gate 11 (build) PASS
- interactions_legacy drop deferred and blocker logged
- Ready to proceed to Plan 05 (SCHEMA.md docs)
</success_criteria>

<output>
After completion, create `/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-04-SUMMARY.md`
</output>

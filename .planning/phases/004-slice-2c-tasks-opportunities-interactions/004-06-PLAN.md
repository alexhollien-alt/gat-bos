---
phase: 004-slice-2c-tasks-opportunities-interactions
plan: 06
type: execute
wave: 5
depends_on:
  - 004-05
files_modified: []
autonomous: false
requirements:
  - SLICE-2C-09
  - SLICE-2C-10

must_haves:
  truths:
    - "a single clean commit exists for Slice 2C on branch gsd/004-slice-2c-tasks-opportunities-interactions"
    - "git tag slice-2c-complete is present"
    - "a GitHub PR is open for the branch"
  artifacts: []
  key_links:
    - from: "git tag"
      to: "slice-2c-complete"
      via: "git tag slice-2c-complete"
      pattern: "slice-2c-complete"
---

<objective>
Close out Slice 2C with a clean commit, a tag, and a pull request.

1. Stage only the files changed in this slice (do NOT use git add -A or git add .)
2. Commit with the canonical Slice 2C message
3. Tag the commit: git tag slice-2c-complete
4. Push branch and tag to origin
5. Open a PR via gh pr create

This plan is autonomous: false because git push requires network access and confirms
that origin has the branch. Alex must confirm push is intended before it runs.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/ROADMAP.md
@/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-CONTEXT.md
</context>

<tasks>

<task type="manual">
  <name>Task 10a: Stage and commit Slice 2C files</name>
  <files>-- (git operations)</files>
  <read_first>
    - Run: git status (see all modified/untracked files)
    - Run: git diff --stat HEAD (see what changed)
  </read_first>
  <action>
    Stage only the files changed in this slice. Do NOT use git add -A or git add .:

    git add supabase/migrations/20260424100000_slice2c_tasks_extend.sql
    git add supabase/migrations/20260424110000_slice2c_interactions_view.sql
    git add src/lib/captures/promote.ts
    git add src/lib/contact-activity.ts
    git add src/lib/types.ts
    git add src/app/\(app\)/follow-ups/page.tsx
    git add src/components/follow-ups/follow-up-list.tsx
    git add src/components/follow-ups/follow-up-form.tsx
    git add src/lib/action-scoring.ts
    git add src/components/dashboard/task-list.tsx
    git add SCHEMA.md
    git add BUILD.md
    git add BLOCKERS.md

    Add any other files that were actually modified by this slice (check git status).
    Omit .claude/worktrees/ and .playwright-mcp/ (untracked, out of scope).

    Commit with this exact message:
    git commit -m "plumbing(004): Slice 2C -- tasks merge, opportunities consolidation, interactions view"

    After commit: git log --oneline -3 (verify the commit is present)
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - git log --oneline -1 shows: "plumbing(004): Slice 2C -- tasks merge, opportunities consolidation, interactions view"
    - git status shows clean working tree (only .claude/worktrees/ and .playwright-mcp/ untracked)
    - git show --stat HEAD lists the migration files and TypeScript files changed
  </acceptance_criteria>
  <done>Slice 2C commit created on branch gsd/004-slice-2c-tasks-opportunities-interactions</done>
</task>

<task type="manual">
  <name>Task 10b: Tag and push</name>
  <files>-- (git operations)</files>
  <read_first>
    - Run: git tag -l | grep slice (verify existing tags -- should see slice-1-complete, slice-2a-complete, slice-2b-complete)
    - Run: git remote -v (confirm origin is set)
  </read_first>
  <action>
    Create the tag:
      git tag slice-2c-complete

    Push the branch and tag to origin:
      git push origin gsd/004-slice-2c-tasks-opportunities-interactions
      git push origin slice-2c-complete

    Verify tag is present:
      git tag -l | grep slice-2c-complete
    EXPECTED: slice-2c-complete
  </action>
  <verify>
    <automated>git tag -l | grep -x "slice-2c-complete"</automated>
  </verify>
  <acceptance_criteria>
    - git tag -l | grep slice-2c-complete returns "slice-2c-complete"
    - git push exits 0 (branch pushed to origin)
    - git push origin slice-2c-complete exits 0
  </acceptance_criteria>
  <done>Branch and tag pushed to origin</done>
</task>

<task type="manual">
  <name>Task 10c: Open Pull Request</name>
  <files>-- (gh CLI operation)</files>
  <read_first>
    - Run: gh auth status (confirm GitHub CLI is authenticated)
    - Run: git log --oneline -5 (review commits for PR summary)
  </read_first>
  <action>
    Create the PR:

    gh pr create \
      --title "Slice 2C: Tasks merge, opportunities consolidation, interactions view" \
      --body "$(cat <<'EOF'
## Summary

- Extended tasks table with type/source/due_reason/action_hint columns + tasks_type_check constraint
- Merged follow_ups (0 rows) into tasks with type='follow_up'; dropped follow_ups table
- Added 13 deal-specific columns to opportunities; merged deals (0 rows) into opportunities; dropped deals table
- Renamed interactions to interactions_legacy; created interactions VIEW (UNION ALL: legacy rows + activity_events WHERE verb LIKE interaction.%)
- Updated promote.ts to INSERT into interactions_legacy (view not directly insertable)
- Updated all .from("follow_ups") callers to query tasks with .eq("type", "follow_up")
- Updated contact-activity.ts to accept Task[] for follow-ups arm
- BLOCKERS.md: interactions_legacy drop + promote.ts writeEvent() migration deferred to Slice 3

## Verification

- pnpm typecheck: PASS
- pnpm build: PASS
- Acceptance gates 1-8, 10, 11: all PASS

## What is NOT done (deferred to Slice 3)

- interactions_legacy drop (view Part A depends on it)
- Migrating promote.ts interactions INSERT to writeEvent()
- Route thinning

🤖 Generated with Claude Code
EOF
)"

    After PR is created, verify:
      gh pr view (confirm URL and status)
  </action>
  <verify>
    <automated>gh pr view --json url --jq '.url' 2>/dev/null || echo "check gh pr list"</automated>
  </verify>
  <acceptance_criteria>
    - gh pr create exits 0
    - PR URL is returned and accessible
    - PR title is "Slice 2C: Tasks merge, opportunities consolidation, interactions view"
    - PR is open (not draft, not closed)
  </acceptance_criteria>
  <done>PR opened. Slice 2C complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push -> origin | Requires GitHub auth; scope is limited to the slice branch |
| gh pr create -> GitHub API | PR body contains no secrets; standard code review workflow |
</threat_model>

<verification>
Final Slice 2C acceptance gate 9:

```bash
git tag -l | grep slice-2c-complete
# Expected: slice-2c-complete

# Full 11-gate sweep
git tag -l | grep -E "slice-[123]-complete|slice-2[abc]-complete"
# Expected: slice-1-complete, slice-2a-complete, slice-2b-complete, slice-2c-complete
```
</verification>

<success_criteria>
- Commit "plumbing(004): Slice 2C -- tasks merge, opportunities consolidation, interactions view" on branch gsd/004-slice-2c-tasks-opportunities-interactions
- git tag slice-2c-complete present
- PR open on GitHub
- Gate 9 passes: git tag -l | grep slice-2c-complete returns slice-2c-complete
- All 11 acceptance gates PASS (gates 1-8, 10, 11 from Plan 04; gate 9 from this plan)
</success_criteria>

<output>
After completion, create `/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-06-SUMMARY.md`
</output>

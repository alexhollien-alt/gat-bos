---
phase: 004-slice-2c-tasks-opportunities-interactions
plan: 05
type: execute
wave: 5
depends_on:
  - 004-04
files_modified:
  - SCHEMA.md
  - BUILD.md
  - BLOCKERS.md
autonomous: true
requirements:
  - SLICE-2C-08

must_haves:
  truths:
    - "SCHEMA.md reflects current state: follow_ups=dropped, deals=dropped, interactions=view, interactions_legacy=live"
    - "BUILD.md Slice 2C moved to Built with today's date"
    - "BLOCKERS.md open items for interactions_legacy drop deferral are present"
  artifacts:
    - path: "SCHEMA.md"
      provides: "updated entity classification table"
      contains: "dropped"
    - path: "BUILD.md"
      provides: "Slice 2C build record"
      contains: "Slice 2C"
---

<objective>
Update the three documentation files to reflect Slice 2C's completed state:

1. SCHEMA.md: update entity table rows for follow_ups, deals, interactions, interactions_legacy,
   tasks (new columns), opportunities (new columns)
2. BUILD.md: move Slice 2C to ## Built with today's date (2026-04-24)
3. BLOCKERS.md: add two new Open entries (interactions_legacy drop deferral + any others surfaced)
   and remove the "Execute slice-2a-drop-spine.sql" notes if they were left in SCHEMA.md

No code changes in this plan -- docs only.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/ROADMAP.md
@/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 8: Update SCHEMA.md</name>
  <files>SCHEMA.md</files>
  <read_first>
    - /Users/alex/crm/SCHEMA.md (full file -- understand current entity table structure)
  </read_first>
  <action>
    Update the Entity Classification table in SCHEMA.md:

    1. follow_ups row: Status -> 'dropped', Notes -> 'Dropped in Slice 2C. Rows merged into tasks with type=follow_up.'

    2. deals row: Status -> 'dropped', Notes -> 'Dropped in Slice 2C. Rows merged into opportunities.'

    3. interactions row: Status -> 'view', Notes -> 'Replaced with a VIEW over activity_events in Slice 2C. Part A: interactions_legacy (legacy rows). Part B: activity_events WHERE verb LIKE interaction.%. See interactions_legacy row.'

    4. Add new row for interactions_legacy:
       | interactions_legacy | Raw | live | Legacy interactions table preserved in Slice 2C. Referenced by the interactions VIEW Part A. Drop deferred to Slice 3 after promote.ts migrates to writeEvent(). |

    5. Update tasks row notes (or add column reference if one exists):
       Notes -> 'Extended in Slice 2C: added type (todo/follow_up/commitment), source, due_reason, action_hint columns.'

    6. Update opportunities row notes:
       Notes -> 'Extended in Slice 2C: added 13 deal-specific columns (buyer_name, seller_name, earnest_money, commission_rate, escrow_company, escrow_officer, title_company, lender_name, lender_partner_id, contract_date, escrow_open_date, scheduled_close_date, actual_close_date).'

    7. Remove any notes that say "Execute supabase/migrations/slice-2a-drop-spine.sql to finalize" from the dropped spine table rows (signals, focus_queue, cycle_state, commitments, spine_inbox) -- those migrations have been executed.

    Update SCHEMA.md last updated date to 2026-04-24 (Slice 2C).
  </action>
  <verify>
    <automated>grep -n "Slice 2C" /Users/alex/crm/SCHEMA.md</automated>
  </verify>
  <acceptance_criteria>
    - SCHEMA.md contains "Dropped in Slice 2C" for follow_ups
    - SCHEMA.md contains "Dropped in Slice 2C" for deals
    - SCHEMA.md contains "interactions_legacy" as a new row with status 'live'
    - SCHEMA.md interactions row status is 'view'
    - SCHEMA.md tasks row notes mention Slice 2C column additions
    - SCHEMA.md last updated date is 2026-04-24
    - grep "Execute supabase/migrations/slice-2a-drop-spine.sql" returns NO matches (removed)
  </acceptance_criteria>
  <done>SCHEMA.md updated to reflect Slice 2C state</done>
</task>

<task type="auto">
  <name>Task 9a: Update BUILD.md</name>
  <files>BUILD.md</files>
  <read_first>
    - /Users/alex/crm/BUILD.md (full file -- understand current structure for Currently Building and Built)
  </read_first>
  <action>
    Add a new entry under ## Built (insert at top of the Built list, most recent first):

    - [2026-04-24] **Slice 2C -- Tasks merge, opportunities consolidation, interactions view (plumbing session).**
      Extended tasks table with type/source/due_reason/action_hint columns and tasks_type_check constraint.
      Merged follow_ups rows (0) into tasks with type='follow_up'; dropped follow_ups.
      Added 13 deal-specific columns to opportunities; merged deals rows (0) into opportunities; dropped deals.
      Renamed interactions to interactions_legacy; created interactions VIEW (UNION ALL of legacy rows +
      activity_events WHERE verb LIKE 'interaction.%').
      Updated promote.ts to write to interactions_legacy; all .from("follow_ups") callers updated to tasks.
      Migration files: 20260424100000_slice2c_tasks_extend.sql + 20260424110000_slice2c_interactions_view.sql.
      pnpm typecheck PASS, pnpm build PASS.

    Update ## Currently Building to reflect the next recommended session:
    (Keep the existing "Universal agent welcome email" note in Currently Building -- that is still next.)
  </action>
  <verify>
    <automated>grep -n "Slice 2C" /Users/alex/crm/BUILD.md</automated>
  </verify>
  <acceptance_criteria>
    - BUILD.md ## Built contains [2026-04-24] Slice 2C entry
    - Entry mentions tasks, follow_ups, deals, opportunities, interactions
    - BUILD.md ## Currently Building still references the welcome email (unchanged)
  </acceptance_criteria>
  <done>BUILD.md updated with Slice 2C built entry</done>
</task>

<task type="auto">
  <name>Task 9b: Update BLOCKERS.md with new open items</name>
  <files>BLOCKERS.md</files>
  <read_first>
    - /Users/alex/crm/BLOCKERS.md (full file -- review Open section for any Slice 2C notes already added in Plan 03)
  </read_first>
  <action>
    Verify the promote.ts interactions_legacy entry was added in Plan 03 (Task 6e).
    If it is missing, add it now.

    Add a second new entry under ## Open:

    ### [2026-04-24] interactions_legacy table -- drop deferred to Slice 3
    - **Broken:** The interactions VIEW Part A references interactions_legacy. Dropping interactions_legacy now would break the view. Promote.ts also still writes directly to interactions_legacy (see the promote.ts blocker above).
    - **Where:** supabase (interactions_legacy table exists in public schema). interactions VIEW depends on it.
    - **Fix needed:** (1) Migrate promote.ts to write via writeEvent() with verb='interaction.{type}' and context containing contact_id+summary. (2) Once all writes go through activity_events, rewrite interactions VIEW to remove Part A (UNION ALL interactions_legacy) -- or simply include interactions_legacy in Part A via activity_events backfill. (3) Then DROP TABLE IF EXISTS interactions_legacy CASCADE. Safe to drop once VIEW no longer references it.
  </action>
  <verify>
    <automated>grep -n "interactions_legacy" /Users/alex/crm/BLOCKERS.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - BLOCKERS.md Open contains at least 2 new entries from Slice 2C
    - Entry for promote.ts direct INSERT to interactions_legacy is present
    - Entry for interactions_legacy drop deferral is present
    - No Slice 2C items in ## Resolved yet (resolution happens after Slice 3 cleans them up)
  </acceptance_criteria>
  <done>BLOCKERS.md updated with Slice 2C open items</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | No sensitive data; plain markdown. No injection risk. |
</threat_model>

<verification>
After this plan completes:

```bash
grep -n "Slice 2C" ~/crm/SCHEMA.md | head -10
# Expected: multiple matches (follow_ups, deals, interactions, tasks, opportunities rows)

grep -n "Slice 2C" ~/crm/BUILD.md | head -5
# Expected: 1 match in ## Built section

grep -n "interactions_legacy" ~/crm/BLOCKERS.md | head -5
# Expected: 2+ matches (promote.ts blocker + drop deferral blocker)
```
</verification>

<success_criteria>
- SCHEMA.md updated with all Slice 2C entity changes
- BUILD.md Slice 2C entry added to Built
- BLOCKERS.md has 2 new Open entries from Slice 2C
- Ready for Plan 06 (commit + tag + PR)
</success_criteria>

<output>
After completion, create `/Users/alex/crm/.planning/phases/004-slice-2c-tasks-opportunities-interactions/004-05-SUMMARY.md`
</output>

---
phase: 023
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - /tmp/design-audit-YYYY-MM-DD/crm/direction-B-locked-spec.md
autonomous: true
requirements:
  - AUDIT-CRM-03

must_haves:
  truths:
    - "direction-B-locked-spec.md exists at $AUDIT_DIR/crm/ and is the audit source of truth."
    - "Spec lists warm charcoal background, champagne gold accent, Instrument Serif numerals, Inter UI text, surgical crimson, integrated calendar, Today's Focus dominance, Runway interaction model."
    - "Any spec value sourced from a fallback (not from design-tokens skill) is fill-and-flagged in the spec body."
  artifacts:
    - path: "/tmp/design-audit-{date}/crm/direction-B-locked-spec.md"
      provides: "Locked Direction B Concierge spec used by every Wave 2 critique."
      min_lines: 40
      contains: "warm charcoal"
  key_links:
    - from: "$AUDIT_DIR/crm/direction-B-locked-spec.md"
      to: "design-critique Mode A (Wave 2)"
      via: "Wave 2 tasks pass this spec path as the locked-spec input"
      pattern: "direction-B-locked-spec.md"
---

<objective>
Write the locked Direction B Concierge spec to disk as a single markdown file. This is the source of truth that every Wave 2 design-critique run compares against. No code, no captures; pure spec authoring.

Purpose: design-critique Mode A is rubric-driven. Without a written spec on disk, every critique invents its own baseline and findings are not comparable. Writing the spec once unifies all critiques.
Output: `$AUDIT_DIR/crm/direction-B-locked-spec.md`.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md
@/Users/alex/.claude/skills/design-tokens/SKILL.md
@/Users/alex/.claude/context/colors.md
@/Users/alex/.claude/context/typography.md

<interfaces>
This plan does not consume code interfaces. It writes one markdown file. Downstream Wave 2 tasks reference the file by absolute path:

```
SPEC=$AUDIT_DIR/crm/direction-B-locked-spec.md
```

Spec section template (Wave 2 critiques grep these headings):

```
# Direction B Concierge, Locked Spec

## Color
- Background: warm charcoal #1A1714
- Accent: champagne gold #C9A961 (fill-and-flag if not in design-tokens)
- Urgency only: crimson #b31a35

## Typography
- Display numerals: Instrument Serif 700, tight letter spacing
- UI text: Inter 400, generous line height
- Metrics: Inter 600

## Layout
- Calendar widget: top-right, integrated (not floating)
- Today's Focus: dominant, above analytics
- Runway interaction model: 6-10 tasks visible, system-ordered by effort, collapse on complete

## Crimson policy
- Used only for urgency states
- Never decorative
- Never on background or non-alert chrome
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 023-02-01: Write Direction B locked spec to $AUDIT_DIR/crm/direction-B-locked-spec.md</name>
  <files>/tmp/design-audit-YYYY-MM-DD/crm/direction-B-locked-spec.md</files>
  <read_first>
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md (Locked Direction B spec section: warm charcoal #1A1714, champagne gold accent, Instrument Serif numerals, Inter text, surgical crimson, integrated calendar, Today's Focus priority, Runway model)
    - /Users/alex/.claude/skills/design-tokens/SKILL.md (source of truth for champagne gold value; if absent or mismatched, fall back to working value #C9A961 and flag)
    - /Users/alex/.claude/context/colors.md (system palette context for double-checking warm charcoal value)
    - /Users/alex/.claude/context/typography.md (Instrument Serif / Inter availability)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (token-vs-Direction-B misalignment finding; spec must reflect the LOCKED target state, not the current code state)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env` to load AUDIT_DIR. Create the file `$AUDIT_DIR/crm/direction-B-locked-spec.md` with the following sections in this exact order:

    1. `# Direction B Concierge, Locked Spec`, followed by a one-paragraph framing line: this file is the audit baseline; current code may diverge.

    2. `## Color`
       - `Background: warm charcoal #1A1714` (verbatim string, downstream grep depends on it)
       - `Accent: champagne gold #C9A961` (if design-tokens skill declares a different hex, use that and add a fill-and-flag line right below: `[FILL AND FLAG: champagne gold sourced from working value; design-tokens skill at /Users/alex/.claude/skills/design-tokens/SKILL.md declares X]`)
       - `Crimson urgency only: #b31a35`

    3. `## Typography`
       - `Display numerals: Instrument Serif 700, tight letter spacing`
       - `UI text: Inter 400, generous line height`
       - `Metrics: Inter 600`

    4. `## Layout`
       - `Calendar widget: top-right, integrated (not floating)`
       - `Today's Focus: dominant, above analytics`
       - `Runway interaction model: 6-10 tasks visible, system-ordered by effort, collapse on complete`

    5. `## Crimson usage policy`
       - `Used only for urgency states (overdue, escalation, blocked)`
       - `Never decorative`
       - `Never on background, headers, or non-alert chrome`
       - `Ratio target: less than 5 percent of visible chroma`

    6. `## Critique grep keys`, listed as a fenced code block so Wave 2 has a known target list to scan against:
       ```
       warm charcoal
       champagne gold
       Instrument Serif
       Inter
       Crimson
       Calendar widget integrated
       Today's Focus dominant
       Runway interaction model
       ```

    Use double hyphens, commas, semicolons, periods. NO em dashes. NO en dashes. If quoting CONTEXT.md text, transcribe the punctuation as ASCII.

    Source values from skills where possible. When sourcing a working value, add `[FILL AND FLAG: ...]` on the same line so Wave 5 synthesis can pick up unresolved values.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/direction-B-locked-spec.md" && grep -q "warm charcoal" "$AUDIT_DIR/crm/direction-B-locked-spec.md" && grep -q "Instrument Serif" "$AUDIT_DIR/crm/direction-B-locked-spec.md" && grep -q "Runway interaction model" "$AUDIT_DIR/crm/direction-B-locked-spec.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/direction-B-locked-spec.md"` returns true (file exists and is non-empty)
    - `grep -q "warm charcoal" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` succeeds
    - `grep -q "champagne gold" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` succeeds
    - `grep -q "Instrument Serif" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` succeeds
    - `grep -q "Runway interaction model" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` succeeds
    - `grep -q "## Critique grep keys" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` succeeds
    - `grep -P "[\x{2014}\x{2013}]" "$AUDIT_DIR/crm/direction-B-locked-spec.md"` returns no matches
  </acceptance_criteria>
  <done>
    Spec file exists at $AUDIT_DIR/crm/direction-B-locked-spec.md with all six required sections, every spec value present, fill-and-flag annotations applied to any working-value fallbacks, and no banned dash characters.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are crossed by this plan; it writes one markdown file to `/tmp/` based on values from local skill files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-023-03 | I (Information disclosure) | spec contents | accept | Spec contains brand color hex codes and typography names already documented in public-readable skill files. No secrets. |
</threat_model>

<verification>
- `test -s "$AUDIT_DIR/crm/direction-B-locked-spec.md"` returns true
- File contains all six required section headings and the critique grep keys block
- File contains no em or en dash characters
</verification>

<success_criteria>
Direction B locked spec written to disk. Wave 2 critique tasks have a stable baseline to compare every captured PNG against.
</success_criteria>

<output>
After completion, write `.planning/phases/023-design-overhaul-crm-audit/023-02-SUMMARY.md` recording the spec file path, the champagne gold value used (and whether it was sourced from design-tokens or working value), and any fill-and-flag annotations applied.
</output>

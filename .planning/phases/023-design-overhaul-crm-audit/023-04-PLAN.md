---
phase: 023
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-1440x900.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-390x844.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-1440x900.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-390x844.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-1440x900.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-390x844.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-1440x900.md
  - /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-390x844.md
autonomous: true
requirements:
  - AUDIT-CRM-04

must_haves:
  truths:
    - "One critique markdown file exists for every PNG captured in Wave 1."
    - "Every critique addresses the 6-criterion rubric: Token compliance, Typography hierarchy, Spatial rhythm, Crimson usage, Calendar widget integration, Today's Focus hierarchy."
    - "Critiques are read against direction-B-locked-spec.md, not the current code state."
  artifacts:
    - path: "/tmp/design-audit-{date}/crm/critique-{route}-{viewport}.md"
      provides: "design-critique Mode A output, one per captured PNG, 8 files total"
      contains: "Token compliance"
  key_links:
    - from: "$AUDIT_DIR/crm/critique-*.md"
      to: "$AUDIT_DIR/crm/PATTERNS.md (Wave 3)"
      via: "Wave 5 reads every critique file to synthesize system-level vs component-level patterns"
      pattern: "critique-.*\\.md"
---

<objective>
Run design-critique Mode A against every PNG captured in Wave 1, using direction-B-locked-spec.md as the baseline. Produce one critique markdown per PNG (8 total, since /events was fill-and-flagged as missing) under $AUDIT_DIR/crm/.

Purpose: Mode A converts visual deltas between captured state and locked spec into structured punchlists. Wave 5 synthesis reads these to identify patterns.
Output: 8 critique markdown files following the 6-criterion rubric.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md
@/Users/alex/.claude/skills/design-critique/SKILL.md
@/Users/alex/.claude/skills/design-critique/references/mode-a-incoming.md
@/Users/alex/.claude/skills/design-critique/references/rubric.md
@/Users/alex/.claude/skills/design-critique/references/output-template.md

<interfaces>
design-critique Mode A inputs:
- A rendered design file (here: PNG)
- A baseline (here: $AUDIT_DIR/crm/direction-B-locked-spec.md)

Mode A output: structured punchlist markdown. For this phase the executor saves each critique to a deterministic filename:

```
$AUDIT_DIR/crm/critique-<route>-<viewport>.png  -> $AUDIT_DIR/crm/critique-<route>-<viewport>.md
```

The 6-criterion rubric every critique must address (literal section headings used by Wave 5 grep):

1. ## Token compliance
2. ## Typography hierarchy
3. ## Spatial rhythm
4. ## Crimson usage
5. ## Calendar widget integration
6. ## Today's Focus hierarchy

Routes and viewports to critique (8 PNGs):
- dashboard-1440x900, dashboard-390x844
- contacts-1440x900, contacts-390x844
- contact-detail-1440x900, contact-detail-390x844
- alexhollienco-1440x900, alexhollienco-390x844

/events is excluded (file is events-missing.md, not a PNG).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 023-04-01: Run design-critique Mode A on every Wave 1 PNG</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-1440x900.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-390x844.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-1440x900.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-390x844.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-1440x900.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-390x844.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-1440x900.md,
    /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-390x844.md
  </files>
  <read_first>
    - /Users/alex/.claude/skills/design-critique/SKILL.md (Mode A workflow entry point)
    - /Users/alex/.claude/skills/design-critique/references/mode-a-incoming.md (Mode A workflow detail)
    - /Users/alex/.claude/skills/design-critique/references/rubric.md (6-criterion rubric definitions)
    - /Users/alex/.claude/skills/design-critique/references/output-template.md (punchlist format)
    - /tmp/design-audit-YYYY-MM-DD/crm/direction-B-locked-spec.md (baseline; written in Wave 1 plan 02)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md (the 6 critique criteria are listed verbatim in the Critique section)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Load the locked spec once: `SPEC="$AUDIT_DIR/crm/direction-B-locked-spec.md"`.

    For each of the 8 PNGs in `$AUDIT_DIR/crm/`, invoke design-critique Mode A. The skill is image-and-spec driven: it reads the PNG, compares against the baseline spec, writes a structured punchlist. Per the skill's output-template, each critique file MUST contain these literal H2 headings in order:

    ```
    ## Token compliance
    ## Typography hierarchy
    ## Spatial rhythm
    ## Crimson usage
    ## Calendar widget integration
    ## Today's Focus hierarchy
    ```

    Plus a top-line `# Critique: <route> at <viewport>` header and a `## Disposition` footer with one of three values (PRESERVE / REBUILD / EXTRACT) and a one-line rationale.

    For the production alexhollienco captures, criteria 5 (Calendar widget) and 6 (Today's Focus) do not apply (marketing site has no calendar or focus list). Use the heading anyway and write the body as: `Not applicable to marketing site; criterion skipped per Wave 2 contract.` The headings must still grep-match in Wave 5.

    Iterate explicitly over each PNG; do not batch into a single critique. Concrete loop:

    ```
    for ROUTE_VIEWPORT in dashboard-1440x900 dashboard-390x844 contacts-1440x900 contacts-390x844 contact-detail-1440x900 contact-detail-390x844 alexhollienco-1440x900 alexhollienco-390x844; do
      PNG="$AUDIT_DIR/crm/${ROUTE_VIEWPORT}.png"
      OUT="$AUDIT_DIR/crm/critique-${ROUTE_VIEWPORT}.md"
      # invoke design-critique Mode A: PNG vs SPEC, write to OUT
    done
    ```

    The critique itself is the work of design-critique Mode A; this plan supplies the iteration and file targets. Each critique should be specific, naming what value differs from spec (for example: "Background is `var(--background)` resolving to near-black `#0A0A0A`, not warm charcoal `#1A1714`; token drift").

    NO em dashes. NO en dashes. Double hyphens, commas, semicolons, periods.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && [ "$(ls $AUDIT_DIR/crm/critique-*.md | wc -l)" -ge 8 ] && for f in $AUDIT_DIR/crm/critique-*.md; do grep -q "## Token compliance" "$f" && grep -q "## Typography hierarchy" "$f" && grep -q "## Spatial rhythm" "$f" && grep -q "## Crimson" "$f" && grep -q "## Calendar widget" "$f" && grep -q "## Today" "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls "$AUDIT_DIR/crm/critique-"*.md | wc -l` returns at least 8
    - For every critique file, all six H2 headings present (Token compliance, Typography hierarchy, Spatial rhythm, Crimson usage, Calendar widget integration, Today's Focus hierarchy)
    - Every critique file contains a Disposition footer with one of PRESERVE, REBUILD, or EXTRACT
    - `grep -lP "[\x{2014}\x{2013}]" "$AUDIT_DIR/crm/"critique-*.md` returns no matches
    - Critique count is greater than or equal to PNG count: `[ $(ls $AUDIT_DIR/crm/critique-*.md | wc -l) -ge $(ls $AUDIT_DIR/crm/*.png | wc -l) ]`
  </acceptance_criteria>
  <done>
    8 critique markdown files exist. Each addresses the full 6-criterion rubric (heading-checked). Each ends with a Disposition footer. No critique file contains em or en dashes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. Critique reads local PNGs against a local spec, writes local markdown. No network egress (assuming design-critique Mode A is image-and-spec local, no remote calls).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-023-05 | I (Information disclosure) | critique markdown may transcribe contact PII visible in the captured PNG | accept | Local-only artifacts under /tmp/. Same disposition as T-023-04 for the source PNG. Wave 5 synthesis filters PII before any artifact moves out of /tmp/. |
</threat_model>

<verification>
- All 8 critique files exist and are non-empty
- All 6 H2 headings present in every critique file
- Critique count is greater than or equal to PNG count
- No em or en dashes anywhere in the critique files
</verification>

<success_criteria>
Every captured PNG has a paired critique markdown applying the locked Direction B spec via the 6-criterion rubric. Wave 5 synthesis has its input set complete.
</success_criteria>

<output>
After completion, write `.planning/phases/023-design-overhaul-crm-audit/023-04-SUMMARY.md` recording the count of critique files produced, the disposition distribution (how many PRESERVE / REBUILD / EXTRACT), and any critique that flagged a value as fill-and-flag (unresolved).
</output>

---
phase: 023
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - /tmp/design-audit-YYYY-MM-DD/00-environment-gaps.md
  - /tmp/design-audit-YYYY-MM-DD/auth-state.json
  - /tmp/design-audit-YYYY-MM-DD/AUDIT_DIR.env
  - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh
autonomous: false
requirements:
  - AUDIT-CRM-01
user_setup:
  - service: supabase-local-session
    why: "Playwright must hold a valid Supabase session to bypass middleware redirect on (app)/* routes. Cookie acquisition needs a live human login."
    env_vars: []
    dashboard_config:
      - task: "Alex completes interactive login at http://localhost:3000/login (or 3001), then approves checkpoint to save storageState."
        location: "Local browser launched by playwright-cli during checkpoint task."

must_haves:
  truths:
    - "AUDIT_DIR exists at /tmp/design-audit-{date} and is reusable across all subsequent tasks."
    - "Playwright storageState contains a real Supabase session cookie, written to $AUDIT_DIR/auth-state.json."
    - "verify-audit.sh exists and asserts file presence + non-zero byte count for the audit artifacts."
    - "Any missing skill or rule path is recorded in 00-environment-gaps.md, not silently skipped."
  artifacts:
    - path: "/tmp/design-audit-{date}/AUDIT_DIR.env"
      provides: "Single source of truth for the audit directory path, sourced by every downstream task."
    - path: "/tmp/design-audit-{date}/auth-state.json"
      provides: "Playwright storageState with live Supabase cookies."
    - path: ".planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh"
      provides: "Bash verifier for the per-task acceptance matrix in 023-VALIDATION.md."
  key_links:
    - from: "$AUDIT_DIR/auth-state.json"
      to: "playwright-cli --storage-state flag"
      via: "downstream capture tasks load this file before navigating to /dashboard, /contacts, /contacts/[id]"
      pattern: "--storage-state.*auth-state.json"
---

<objective>
Establish the audit environment: probe the running CRM dev server, verify required skill files exist, create the dated AUDIT_DIR under /tmp/, run a one-time interactive Playwright login to capture a Supabase session storageState, and author scripts/verify-audit.sh. This plan is Wave 0 and BLOCKS every downstream wave.

Purpose: Without auth-state.json, every screenshot in Wave 1 records the /login form instead of the CRM. Without AUDIT_DIR exported, downstream tasks have no agreed path. Without verify-audit.sh, the validation matrix has no executor.
Output: AUDIT_DIR path, auth-state.json, environment-gaps.md (if any gaps), verify-audit.sh.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-VALIDATION.md
@/Users/alex/crm/CLAUDE.md
@/Users/alex/.claude/skills/playwright-cli/SKILL.md
@/Users/alex/.claude/skills/playwright-cli/storage-and-auth.md

<interfaces>
Key paths and conventions downstream tasks rely on:

```
AUDIT_DIR=/tmp/design-audit-$(date +%Y-%m-%d)
$AUDIT_DIR/auth-state.json                # Playwright storageState
$AUDIT_DIR/AUDIT_DIR.env                  # exports AUDIT_DIR for source-ability
$AUDIT_DIR/00-environment-gaps.md         # gaps log (only if any check fails)
$AUDIT_DIR/crm/                           # subdir holds captures, critiques, PATTERNS.md
.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh
```

Playwright invocation pattern for Wave 1 captures:
```
playwright-cli screenshot \
  --url http://localhost:PORT/dashboard \
  --viewport 1440x900 \
  --full-page \
  --storage-state $AUDIT_DIR/auth-state.json \
  --out $AUDIT_DIR/crm/dashboard-1440x900.png
```

Path drift note: CONTEXT.md references ~/.claude/rules/digital-aesthetic.md and ~/.claude/rules/design-foundation.md. Per research finding, both files actually live at ~/.claude/context/. Record this drift in 00-environment-gaps.md but DO NOT halt; the audit references them via the context/ path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 023-01-01: Environment check + AUDIT_DIR setup</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/AUDIT_DIR.env,
    /tmp/design-audit-YYYY-MM-DD/00-environment-gaps.md (only if gaps),
    /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh
  </files>
  <read_first>
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md (Pre-work section, environment checklist)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-VALIDATION.md (Wave 0 Requirements; per-task verification map)
    - /Users/alex/crm/CLAUDE.md (Standing Rule 17: probe port 3000 AND 3001 before assuming dev server location)
    - /Users/alex/.claude/skills/design-critique/SKILL.md (confirm Mode A documented; needed for Wave 2)
    - /Users/alex/.claude/skills/playwright-cli/SKILL.md (confirm CLI available)
  </read_first>
  <action>
    Set AUDIT_DIR via `AUDIT_DIR=/tmp/design-audit-$(date +%Y-%m-%d)` and run `mkdir -p "$AUDIT_DIR/crm"`. Write `$AUDIT_DIR/AUDIT_DIR.env` with one line: `export AUDIT_DIR=/tmp/design-audit-YYYY-MM-DD` (substituting the resolved date). Every subsequent task sources this file.

    Run each environment check in order; if a check fails, append a single line to `$AUDIT_DIR/00-environment-gaps.md` (create on first failure) recording the gap and continue (fill-and-flag per standing rule 1). Do not stop on a missing skill file.

    Checks:
    1. `npx playwright --version`, record version. If not installed, log gap and run `npx playwright install chromium`.
    2. Probe dev server: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login` AND `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/login`. Note which responds 200. If neither, start `pnpm dev` in `~/crm/` and re-probe. Record the active port in `$AUDIT_DIR/AUDIT_DIR.env` as `export CRM_PORT=3000` (or 3001).
    3. `test -f /Users/alex/.claude/skills/design-generator/SKILL.md`, log gap if missing.
    4. `test -f /Users/alex/.claude/skills/design-critique/SKILL.md && grep -q "Mode A" /Users/alex/.claude/skills/design-critique/SKILL.md && grep -q "Mode B" /Users/alex/.claude/skills/design-critique/SKILL.md`, log gap if any miss.
    5. `test -f /Users/alex/.claude/skills/brand-audit/SKILL.md`, log gap if missing.
    6. `test -f /Users/alex/.claude/skills/design-tokens/SKILL.md`, log gap if missing.
    7. `test -f /Users/alex/.claude/context/digital-aesthetic.md` (research-confirmed correct path; CONTEXT.md says rules/ which is wrong). Log the path drift in 00-environment-gaps.md regardless: "CONTEXT.md references ~/.claude/rules/digital-aesthetic.md; actual location is ~/.claude/context/digital-aesthetic.md. Downstream tasks use context/ path."
    8. `test -f /Users/alex/.claude/context/design-foundation.md`, same drift note.
    9. `test -f /Users/alex/.claude/rules/brand.md && grep -q "b31a35" /Users/alex/.claude/rules/brand.md` per CONTEXT.md (GAT Red locked). If `b31a35` not present in brand.md, log gap noting where Direction B crimson value is actually canonical (research says design-tokens skill).

    Author `.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` per the validation matrix. Script must:
    - Source `$AUDIT_DIR/AUDIT_DIR.env`.
    - Assert non-zero-byte existence for: auth-state.json; crm/direction-B-locked-spec.md; crm/dashboard-1440x900.png and 390x844 variants; crm/contacts-{1440x900,390x844}.png; crm/contact-detail-{1440x900,390x844}.png; crm/events-missing.md (file existence, may be empty placeholder); crm/alexhollienco-{1440x900,390x844}.png.
    - Count `crm/critique-*.md` files; assert count is greater than or equal to count of `crm/*.png`.
    - Grep `crm/PATTERNS.md` for both "## System-level violations" and "## Component-level violations".
    - Exit 0 on full pass; exit 1 with a clear stderr line per missing artifact.
    - Make executable: `chmod +x scripts/verify-audit.sh`.

    NO em dashes or en dashes anywhere in the script or gaps file. Use double hyphens, commas, semicolons.
  </action>
  <verify>
    <automated>test -f "$(cat /tmp/design-audit-*/AUDIT_DIR.env | head -1 | cut -d= -f2)/AUDIT_DIR.env" && test -x /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh && bash -n /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -d "$AUDIT_DIR"` returns true
    - `test -f "$AUDIT_DIR/AUDIT_DIR.env"` returns true and file contains `export AUDIT_DIR=` and `export CRM_PORT=` lines
    - `test -x /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` returns true
    - `bash -n .../verify-audit.sh` exits 0 (syntax valid)
    - If any check failed, `test -f "$AUDIT_DIR/00-environment-gaps.md"` returns true; otherwise the file may be absent
    - `grep -P "[\x{2014}\x{2013}]" "$AUDIT_DIR/00-environment-gaps.md" .planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` returns no matches (no em or en dashes; uses Perl regex hex escapes for the two banned codepoints)
  </acceptance_criteria>
  <done>
    AUDIT_DIR established, env file written with CRM_PORT, verify-audit.sh syntax-valid and executable, all environment checks recorded (passes silent, fails logged to 00-environment-gaps.md). Path drift for context/ vs rules/ logged whether or not files exist.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 023-01-02: Capture Playwright auth storageState (one-time interactive login)</name>
  <files>/tmp/design-audit-YYYY-MM-DD/auth-state.json</files>
  <read_first>
    - /Users/alex/.claude/skills/playwright-cli/storage-and-auth.md (storageState save/load pattern)
    - /Users/alex/.claude/skills/playwright-cli/SKILL.md (CLI entry points)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (auth-wall finding; middleware redirect behavior)
    - /Users/alex/crm/middleware.ts (confirm redirect rule still in effect)
  </read_first>
  <what-built>
    Task 023-01-01 staged AUDIT_DIR, the environment env file, and verify-audit.sh. The dev server is confirmed responsive on the recorded CRM_PORT. The Playwright skill is verified loadable.
  </what-built>
  <how-to-verify>
    Source the env: `source "$AUDIT_DIR/AUDIT_DIR.env"`. Then run the interactive Playwright login:

    ```
    playwright-cli session save \
      --url "http://localhost:${CRM_PORT}/login" \
      --out "$AUDIT_DIR/auth-state.json"
    ```

    (If the playwright-cli wrapper exposes a different subcommand for storageState save, follow storage-and-auth.md; the canonical pattern is "headed Chromium opens, user completes login, storageState saves on close.")

    1. Headed Chromium opens at http://localhost:PORT/login.
    2. Alex enters the same Supabase credentials he uses day-to-day.
    3. Once redirected to /dashboard (confirming session is live), Alex closes the browser window.
    4. Playwright writes the cookies + localStorage to `$AUDIT_DIR/auth-state.json`.
    5. Verify: `test -s "$AUDIT_DIR/auth-state.json" && grep -q "cookies" "$AUDIT_DIR/auth-state.json"`.

    Expected: file exists, non-empty, contains a JSON object with a `cookies` array including at least one entry for the Supabase auth cookie (name typically starts with `sb-`).

    Do NOT commit `$AUDIT_DIR` to git. It lives in /tmp/ and contains a live session token.
  </how-to-verify>
  <resume-signal>Reply "approved" after the file exists and verification passes, or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser to local CRM | Supabase session cookie passes from interactive login into `$AUDIT_DIR/auth-state.json`. The file holds a real bearer token. |
| /tmp filesystem to git working tree | `$AUDIT_DIR` lives at `/tmp/design-audit-YYYY-MM-DD/`. Must never be moved, symlinked, or copied into `~/crm/` or any path tracked by git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-023-01 | I (Information disclosure) | `$AUDIT_DIR/auth-state.json` containing live Supabase session cookie | mitigate | Path is hardcoded under `/tmp/`, which is not tracked by `.gitignore` because it is not inside the repo. verify-audit.sh asserts presence but never `cat`s the file. Downstream capture tasks reference the file via `--storage-state` flag only; never log its contents. The plan explicitly forbids copying the file out of `/tmp/`. End-of-phase `/clear` plus OS reboot wipe `/tmp/`. |
| T-023-02 | T (Tampering) | verify-audit.sh tampering could mask a missing artifact | accept | Script lives inside the repo at `.planning/phases/023-design-overhaul-crm-audit/scripts/`. Modification flows through normal git review. Low risk for an audit-evidence phase. |
</threat_model>

<verification>
- `test -d "$AUDIT_DIR" && test -f "$AUDIT_DIR/AUDIT_DIR.env" && test -s "$AUDIT_DIR/auth-state.json"`
- `bash -n .planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` exits 0
- `chmod +x .planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` already applied
- If `$AUDIT_DIR/00-environment-gaps.md` exists, it has at least one entry and uses no em or en dashes
</verification>

<success_criteria>
Wave 0 complete: AUDIT_DIR set, verify-audit.sh authored and syntax-valid, auth-state.json captured with a real Supabase session, all environment checks logged (gaps or silent pass). Wave 1 plans (02 and 03) are now unblocked.
</success_criteria>

<output>
After completion, write `.planning/phases/023-design-overhaul-crm-audit/023-01-SUMMARY.md` recording AUDIT_DIR path, CRM_PORT detected, list of environment gaps logged (if any), and confirmation that auth-state.json is present and non-empty.
</output>

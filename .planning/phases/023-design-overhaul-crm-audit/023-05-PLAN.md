---
phase: 023
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - /tmp/design-audit-YYYY-MM-DD/crm/PATTERNS.md
autonomous: true
requirements:
  - AUDIT-CRM-05

must_haves:
  truths:
    - "PATTERNS.md exists at $AUDIT_DIR/crm/ and is the single GATE 1 deliverable."
    - "PATTERNS.md separates system-level violations (3+ routes affected) from component-level violations (1-2 routes affected)."
    - "PATTERNS.md names the underlying CSS variable or token responsible for each system-level violation when applicable, sourced from the captured evidence and the design-tokens skill, not invented."
    - "PATTERNS.md ends with a GATE 1 STOP line; no fixes are proposed or applied."
  artifacts:
    - path: "/tmp/design-audit-{date}/crm/PATTERNS.md"
      provides: "GATE 1 synthesis. Phase 027 remediation reads this as its source."
      min_lines: 30
      contains: "## System-level violations"
  key_links:
    - from: "$AUDIT_DIR/crm/critique-*.md"
      to: "$AUDIT_DIR/crm/PATTERNS.md"
      via: "Wave 5 reads every critique, counts violations per criterion across routes, partitions into system vs component"
      pattern: "## System-level violations"
---

<objective>
Read every Wave 2 critique markdown and synthesize into a single PATTERNS.md that distinguishes system-level violations (recurring across 3+ routes, hence fixable at the token or global CSS layer) from component-level violations (isolated to 1-2 routes, fixable at the component layer). Halt at GATE 1. No fixes.

Purpose: Phase 027 remediation needs a partitioned punchlist to know where to start (tokens? globals.css? one specific component?). PATTERNS.md is that partition.
Output: $AUDIT_DIR/crm/PATTERNS.md.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-VALIDATION.md
@/Users/alex/crm/app/globals.css
@/Users/alex/.claude/skills/design-tokens/SKILL.md

<interfaces>
Inputs (read-only):
- `$AUDIT_DIR/crm/direction-B-locked-spec.md` (the baseline)
- `$AUDIT_DIR/crm/critique-*.md` (8 critique files from Wave 2)
- `$AUDIT_DIR/crm/events-missing.md` (route absence finding)

Output structure (Wave 5 verifier and Phase 027 consumer rely on these literal headings):

```
# CRM Audit Patterns, Direction B Concierge

## Summary
<one paragraph framing, counts of system vs component findings>

## System-level violations
<violations seen in 3+ critique files; one subheading per pattern>

### Pattern: <name>
- Routes affected: <list>
- Direction B lock violated: <quoted spec line>
- Likely source: <CSS variable, token, or global style; sourced from globals.css and design-tokens skill>
- Evidence: <pointer to critique files>

## Component-level violations
<violations seen in 1-2 critique files>

### Component: <route/component name>
- Direction B lock violated: <quoted spec line>
- Evidence: <pointer to critique files>

## IA gaps
- /events: top-level CRM route absent; portal-scoped variant exists. From events-missing.md.

## GATE 1, STOP
This phase exits before remediation. No fixes applied. Phase 027 reads this file as its source.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 023-05-01: Synthesize PATTERNS.md from all critique files</name>
  <files>/tmp/design-audit-YYYY-MM-DD/crm/PATTERNS.md</files>
  <read_first>
    - /tmp/design-audit-YYYY-MM-DD/crm/direction-B-locked-spec.md (baseline, for quoting violated locks)
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-1440x900.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-dashboard-390x844.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-1440x900.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-contacts-390x844.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-1440x900.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-contact-detail-390x844.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-1440x900.md
    - /tmp/design-audit-YYYY-MM-DD/crm/critique-alexhollienco-390x844.md
    - /tmp/design-audit-YYYY-MM-DD/crm/events-missing.md
    - /Users/alex/crm/app/globals.css (source CSS variables to name token-layer fixes)
    - /Users/alex/.claude/skills/design-tokens/SKILL.md (canonical token values to compare against)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Read every critique file. For each of the 6 rubric criteria (Token compliance, Typography hierarchy, Spatial rhythm, Crimson usage, Calendar widget integration, Today's Focus hierarchy), count how many critique files flagged that criterion as a violation.

    Partition violations:
    - Count of routes affected greater than or equal to 3 (with each route counted once even if both viewports flag it): SYSTEM-LEVEL.
    - Count of routes affected 1 or 2: COMPONENT-LEVEL.

    For each SYSTEM-LEVEL pattern, attempt to name the underlying source:
    - Read `~/crm/app/globals.css` for CSS variables. If `--background` resolves to a value that does not match warm charcoal `#1A1714`, that is the token-layer source for any Token compliance pattern.
    - Read `~/.claude/skills/design-tokens/SKILL.md` for canonical values. If the skill declares a different champagne gold than direction-B-locked-spec.md, note the discrepancy.
    - Do NOT propose fixes. Name the source only.

    Write `$AUDIT_DIR/crm/PATTERNS.md` with the exact section structure given in the <interfaces> block above:
    1. `# CRM Audit Patterns, Direction B Concierge`
    2. `## Summary` (one paragraph: counts of system vs component, dominant pattern)
    3. `## System-level violations` (one `### Pattern: <name>` subsection per system pattern, with Routes affected, Direction B lock violated, Likely source, Evidence subfields)
    4. `## Component-level violations` (one `### Component: <route/component>` subsection per component pattern)
    5. `## IA gaps` (transcribe the /events absence from events-missing.md)
    6. `## GATE 1, STOP` (explicit halt line)

    Constraints:
    - Quote the violated Direction B lock verbatim from direction-B-locked-spec.md.
    - Cite evidence as a list of critique filenames; do NOT inline critique text.
    - Likely source must be a real file path (globals.css, a component file, a token skill) or `unknown` if no file maps.
    - NO em dashes. NO en dashes. ASCII punctuation only.
    - NO remediation language. No `should`, no `fix`, no `change to`. Findings only.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/PATTERNS.md" && grep -q "## System-level violations" "$AUDIT_DIR/crm/PATTERNS.md" && grep -q "## Component-level violations" "$AUDIT_DIR/crm/PATTERNS.md" && grep -q "## GATE 1" "$AUDIT_DIR/crm/PATTERNS.md" && ! grep -qP "[\x{2014}\x{2013}]" "$AUDIT_DIR/crm/PATTERNS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/PATTERNS.md"` returns true
    - `grep -q "## System-level violations" "$AUDIT_DIR/crm/PATTERNS.md"` succeeds
    - `grep -q "## Component-level violations" "$AUDIT_DIR/crm/PATTERNS.md"` succeeds
    - `grep -q "## IA gaps" "$AUDIT_DIR/crm/PATTERNS.md"` succeeds
    - `grep -q "## GATE 1" "$AUDIT_DIR/crm/PATTERNS.md"` succeeds
    - `grep -q "events" "$AUDIT_DIR/crm/PATTERNS.md"` succeeds (IA gap from events-missing.md transcribed)
    - `grep -P "[\x{2014}\x{2013}]" "$AUDIT_DIR/crm/PATTERNS.md"` returns no matches
    - At least one Likely source field cites either `globals.css` or the design-tokens skill path
    - Running `bash .planning/phases/023-design-overhaul-crm-audit/scripts/verify-audit.sh` exits 0 (all artifacts present, all assertions pass)
  </acceptance_criteria>
  <done>
    PATTERNS.md written; structure validated; verify-audit.sh exits 0 across the full audit set. GATE 1 reached. Phase 023 exits here; remediation lives in Phase 027.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. Wave 5 reads local markdown and CSS, writes local markdown.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-023-06 | I (Information disclosure) | PATTERNS.md may surface internal product structure (route names, component names, contact PII names from critique evidence) | accept | Local artifact in /tmp/. Alex reviews before any later phase reuses PATTERNS.md in a public artifact. PATTERNS.md cites critique filenames as evidence rather than transcribing PII inline. |
</threat_model>

<verification>
- PATTERNS.md exists, non-empty, has all 5 required sections plus GATE 1 stop line
- verify-audit.sh exits 0 over the full artifact set
- No em or en dashes
- No remediation verbs (`should`, `fix`, `change to`) in PATTERNS.md body
</verification>

<success_criteria>
GATE 1 reached. PATTERNS.md partitions findings into system-level (3+ routes) vs component-level (1-2 routes) plus IA gaps. Phase 023 exits without any remediation. Phase 027 consumer reads PATTERNS.md as its starting point.
</success_criteria>

<output>
After completion, write `.planning/phases/023-design-overhaul-crm-audit/023-05-SUMMARY.md` recording PATTERNS.md path, count of system-level patterns, count of component-level patterns, and a one-line statement confirming GATE 1 reached without remediation.
</output>

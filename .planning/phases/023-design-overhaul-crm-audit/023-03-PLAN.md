---
phase: 023
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - /tmp/design-audit-YYYY-MM-DD/crm/dashboard-1440x900.png
  - /tmp/design-audit-YYYY-MM-DD/crm/dashboard-390x844.png
  - /tmp/design-audit-YYYY-MM-DD/crm/contacts-1440x900.png
  - /tmp/design-audit-YYYY-MM-DD/crm/contacts-390x844.png
  - /tmp/design-audit-YYYY-MM-DD/crm/contact-detail-1440x900.png
  - /tmp/design-audit-YYYY-MM-DD/crm/contact-detail-390x844.png
  - /tmp/design-audit-YYYY-MM-DD/crm/events-missing.md
  - /tmp/design-audit-YYYY-MM-DD/crm/alexhollienco-1440x900.png
  - /tmp/design-audit-YYYY-MM-DD/crm/alexhollienco-390x844.png
autonomous: true
requirements:
  - AUDIT-CRM-01
  - AUDIT-CRM-02

must_haves:
  truths:
    - "Desktop and mobile PNGs exist for /dashboard, /contacts, /contacts/[id]."
    - "Production alexhollienco.com PNGs exist at both viewports (no auth required)."
    - "/events route absence is documented in events-missing.md rather than silently skipped."
    - "Every CRM PNG was captured authenticated; no PNG shows the /login form."
  artifacts:
    - path: "/tmp/design-audit-{date}/crm/dashboard-1440x900.png"
      provides: "Desktop /dashboard capture, authenticated"
    - path: "/tmp/design-audit-{date}/crm/dashboard-390x844.png"
      provides: "Mobile /dashboard capture, authenticated"
    - path: "/tmp/design-audit-{date}/crm/contacts-1440x900.png"
      provides: "Desktop /contacts list capture"
    - path: "/tmp/design-audit-{date}/crm/contacts-390x844.png"
      provides: "Mobile /contacts list capture"
    - path: "/tmp/design-audit-{date}/crm/contact-detail-1440x900.png"
      provides: "Desktop contact detail capture for seed Stephanie Reid (350ef57b-4c09-4952-bf70-87dad5a94d2e)"
    - path: "/tmp/design-audit-{date}/crm/contact-detail-390x844.png"
      provides: "Mobile contact detail capture"
    - path: "/tmp/design-audit-{date}/crm/events-missing.md"
      provides: "Documented absence of /events route; AUDIT-CRM-01 coverage"
    - path: "/tmp/design-audit-{date}/crm/alexhollienco-1440x900.png"
      provides: "Desktop production marketing site capture"
    - path: "/tmp/design-audit-{date}/crm/alexhollienco-390x844.png"
      provides: "Mobile production marketing site capture"
  key_links:
    - from: "$AUDIT_DIR/auth-state.json"
      to: "every (app)/* capture invocation"
      via: "playwright-cli --storage-state flag"
      pattern: "--storage-state.*auth-state.json"
    - from: "Wave 1 PNGs"
      to: "Wave 2 design-critique runs"
      via: "Wave 2 iterates ls $AUDIT_DIR/crm/*.png"
      pattern: "crm/.*\\.png"
---

<objective>
Capture 9 PNG screenshots and 1 fill-and-flag markdown file: /dashboard, /contacts, /contacts/[seed id] at desktop (1440x900) and mobile (390x844), /events absence flagged as markdown, and alexhollienco.com production site at both viewports. All CRM captures use the storageState from Wave 0; production captures need no auth.

Purpose: This is the evidence base for the entire audit. Every Wave 2 critique compares one of these PNGs against direction-B-locked-spec.md.
Output: 9 PNGs + 1 markdown, all under $AUDIT_DIR/crm/.
</objective>

<execution_context>
@/Users/alex/crm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/crm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md
@/Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-VALIDATION.md
@/Users/alex/.claude/skills/playwright-cli/SKILL.md
@/Users/alex/.claude/skills/playwright-cli/screenshots-and-media.md
@/Users/alex/.claude/skills/playwright-cli/storage-and-auth.md

<interfaces>
Canonical capture pattern (every CRM task uses storageState; production omits it):

```
source "$AUDIT_DIR/AUDIT_DIR.env"

# CRM authenticated capture
playwright-cli screenshot \
  --url "http://localhost:${CRM_PORT}/dashboard" \
  --viewport 1440x900 \
  --full-page \
  --storage-state "$AUDIT_DIR/auth-state.json" \
  --out "$AUDIT_DIR/crm/dashboard-1440x900.png"

# Production unauthenticated capture
playwright-cli screenshot \
  --url "https://alexhollienco.com" \
  --viewport 1440x900 \
  --full-page \
  --out "$AUDIT_DIR/crm/alexhollienco-1440x900.png"
```

Seed contact id for /contacts/[id]: 350ef57b-4c09-4952-bf70-87dad5a94d2e (Stephanie Reid, lender, pre-baked in seed data).

Route gap: /events does not exist under src/app/. Only /api/events (API) and /portal/[slug]/(authed)/events (portal) exist. Wave 1 fill-and-flags this in events-missing.md instead of failing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 023-03-01: Capture /dashboard at both viewports</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/crm/dashboard-1440x900.png,
    /tmp/design-audit-YYYY-MM-DD/crm/dashboard-390x844.png
  </files>
  <read_first>
    - /Users/alex/.claude/skills/playwright-cli/screenshots-and-media.md (full-page screenshot flag, viewport syntax)
    - /Users/alex/.claude/skills/playwright-cli/storage-and-auth.md (--storage-state usage)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (middleware redirect; why storageState is required)
  </read_first>
  <action>
    Source the env: `source "$AUDIT_DIR/AUDIT_DIR.env"`. Capture twice, once per viewport, both full-page, both using `--storage-state "$AUDIT_DIR/auth-state.json"`:

    1. `playwright-cli screenshot --url "http://localhost:${CRM_PORT}/dashboard" --viewport 1440x900 --full-page --storage-state "$AUDIT_DIR/auth-state.json" --out "$AUDIT_DIR/crm/dashboard-1440x900.png"`
    2. `playwright-cli screenshot --url "http://localhost:${CRM_PORT}/dashboard" --viewport 390x844 --full-page --storage-state "$AUDIT_DIR/auth-state.json" --out "$AUDIT_DIR/crm/dashboard-390x844.png"`

    After each capture, sanity-check the PNG. The /login form has distinctive markup; if Playwright redirected to /login the PNG dimensions and content will not match a dashboard. If both PNGs come back as the login form, log to `$AUDIT_DIR/00-environment-gaps.md` and re-run Task 023-01-02 to refresh auth-state.json.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/dashboard-1440x900.png" && test -s "$AUDIT_DIR/crm/dashboard-390x844.png"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/dashboard-1440x900.png"` returns true
    - `test -s "$AUDIT_DIR/crm/dashboard-390x844.png"` returns true
    - File magic confirms PNG: `file "$AUDIT_DIR/crm/dashboard-1440x900.png" | grep -q "PNG image"`
  </acceptance_criteria>
  <done>
    Both /dashboard PNGs present, non-empty, valid PNG magic, and (manually skimmed) show dashboard chrome rather than login form.
  </done>
</task>

<task type="auto">
  <name>Task 023-03-02: Capture /contacts list at both viewports</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/crm/contacts-1440x900.png,
    /tmp/design-audit-YYYY-MM-DD/crm/contacts-390x844.png
  </files>
  <read_first>
    - /Users/alex/.claude/skills/playwright-cli/screenshots-and-media.md
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (confirm /contacts is the correct list route, not /agents)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Capture twice with storageState:

    1. `playwright-cli screenshot --url "http://localhost:${CRM_PORT}/contacts" --viewport 1440x900 --full-page --storage-state "$AUDIT_DIR/auth-state.json" --out "$AUDIT_DIR/crm/contacts-1440x900.png"`
    2. `playwright-cli screenshot --url "http://localhost:${CRM_PORT}/contacts" --viewport 390x844 --full-page --storage-state "$AUDIT_DIR/auth-state.json" --out "$AUDIT_DIR/crm/contacts-390x844.png"`
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/contacts-1440x900.png" && test -s "$AUDIT_DIR/crm/contacts-390x844.png"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/contacts-1440x900.png"` returns true
    - `test -s "$AUDIT_DIR/crm/contacts-390x844.png"` returns true
    - `file "$AUDIT_DIR/crm/contacts-1440x900.png" | grep -q "PNG image"`
  </acceptance_criteria>
  <done>
    Both /contacts PNGs present, non-empty, valid PNG magic.
  </done>
</task>

<task type="auto">
  <name>Task 023-03-03: Capture /contacts/[Stephanie Reid] detail at both viewports</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/crm/contact-detail-1440x900.png,
    /tmp/design-audit-YYYY-MM-DD/crm/contact-detail-390x844.png
  </files>
  <read_first>
    - /Users/alex/.claude/skills/playwright-cli/screenshots-and-media.md
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (seed contact id is 350ef57b-4c09-4952-bf70-87dad5a94d2e for Stephanie Reid)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Capture twice with storageState, targeting the seed contact:

    1. `playwright-cli screenshot --url "http://localhost:${CRM_PORT}/contacts/350ef57b-4c09-4952-bf70-87dad5a94d2e" --viewport 1440x900 --full-page --storage-state "$AUDIT_DIR/auth-state.json" --out "$AUDIT_DIR/crm/contact-detail-1440x900.png"`
    2. Same URL, viewport 390x844, out path `contact-detail-390x844.png`.

    If the contact id 404s (seed not yet loaded in local DB), fall back to: navigate to /contacts list, click the first contact row, capture from there. Document the fallback in `$AUDIT_DIR/00-environment-gaps.md`. Do not abort the plan.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/contact-detail-1440x900.png" && test -s "$AUDIT_DIR/crm/contact-detail-390x844.png"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/contact-detail-1440x900.png"` returns true
    - `test -s "$AUDIT_DIR/crm/contact-detail-390x844.png"` returns true
    - `file "$AUDIT_DIR/crm/contact-detail-1440x900.png" | grep -q "PNG image"`
  </acceptance_criteria>
  <done>
    Both contact-detail PNGs present, non-empty, valid PNG magic. Fallback documented if seed not present.
  </done>
</task>

<task type="auto">
  <name>Task 023-03-04: Fill-and-flag absence of /events route</name>
  <files>/tmp/design-audit-YYYY-MM-DD/crm/events-missing.md</files>
  <read_first>
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/023-RESEARCH.md (research-confirmed: /events does not exist; only /api/events and /portal/[slug]/(authed)/events exist)
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md (capture routes section lists /events with "if it exists" qualifier)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Verify research finding with a quick check: `ls /Users/alex/crm/src/app/(app)/events 2>/dev/null` should return nothing. Write `$AUDIT_DIR/crm/events-missing.md` with these contents (no em or en dashes):

    ```
    # /events route, absent at audit time

    Date: (filled by execute step)
    Confirmed missing under src/app/(app)/.

    What exists instead:
    - src/app/api/events/ (API endpoint, not a user-facing route)
    - src/app/portal/[slug]/(authed)/events/ (portal route, scoped to slugged portals)

    AUDIT-CRM-01 coverage decision: route absence is itself an audit finding. Wave 2 critique steps SKIP this filename. Wave 5 synthesis records the absence under system-level concerns in PATTERNS.md (no top-level /events surface exists for CRM users, while a portal-scoped one does, indicating IA gap).

    Source: 023-RESEARCH.md, route mapping section.
    ```

    The file existence itself satisfies VALIDATION.md task 023-03-04. The body documents the gap.
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -f "$AUDIT_DIR/crm/events-missing.md" && grep -q "absent at audit time" "$AUDIT_DIR/crm/events-missing.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "$AUDIT_DIR/crm/events-missing.md"` returns true
    - `grep -q "AUDIT-CRM-01" "$AUDIT_DIR/crm/events-missing.md"` succeeds
    - `grep -q "portal/\[slug\]/(authed)/events" "$AUDIT_DIR/crm/events-missing.md"` succeeds
    - `grep -P "[\x{2014}\x{2013}]" "$AUDIT_DIR/crm/events-missing.md"` returns no matches
  </acceptance_criteria>
  <done>
    events-missing.md written; route absence documented with the two alternate paths and the synthesis instruction for Wave 5.
  </done>
</task>

<task type="auto">
  <name>Task 023-03-05: Capture alexhollienco.com production site at both viewports</name>
  <files>
    /tmp/design-audit-YYYY-MM-DD/crm/alexhollienco-1440x900.png,
    /tmp/design-audit-YYYY-MM-DD/crm/alexhollienco-390x844.png
  </files>
  <read_first>
    - /Users/alex/.claude/skills/playwright-cli/screenshots-and-media.md
    - /Users/alex/crm/.planning/phases/023-design-overhaul-crm-audit/CONTEXT.md (capture list includes alexhollienco.com as production URL)
  </read_first>
  <action>
    Source `$AUDIT_DIR/AUDIT_DIR.env`. Production captures need NO storageState (public marketing site):

    1. `playwright-cli screenshot --url "https://alexhollienco.com" --viewport 1440x900 --full-page --out "$AUDIT_DIR/crm/alexhollienco-1440x900.png"`
    2. Same URL, viewport 390x844, out path `alexhollienco-390x844.png`.

    If the production site triggers Cloudflare 202/403/429 per tool-routing.md, the playwright-cli skill is already the canonical fallback (no further escalation needed; just retry with a brief wait).
  </action>
  <verify>
    <automated>source "$(ls -td /tmp/design-audit-*/AUDIT_DIR.env | head -1)" && test -s "$AUDIT_DIR/crm/alexhollienco-1440x900.png" && test -s "$AUDIT_DIR/crm/alexhollienco-390x844.png"</automated>
  </verify>
  <acceptance_criteria>
    - `test -s "$AUDIT_DIR/crm/alexhollienco-1440x900.png"` returns true
    - `test -s "$AUDIT_DIR/crm/alexhollienco-390x844.png"` returns true
    - `file "$AUDIT_DIR/crm/alexhollienco-1440x900.png" | grep -q "PNG image"`
  </acceptance_criteria>
  <done>
    Both production PNGs present, non-empty, valid PNG magic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright to local CRM | storageState carries a Supabase session cookie into every request. Local-only traffic. |
| Playwright to alexhollienco.com | Unauthenticated public traffic. No tokens leave the machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-023-01 | I (Information disclosure) | storageState file referenced via flag | mitigate | --storage-state flag references the file by path; Playwright does not echo cookies to stdout. All PNG outputs stay in /tmp/. No PNG contains the session cookie value. |
| T-023-04 | I (Information disclosure) | PNGs may show real contact PII (Stephanie Reid contact card) | accept | Local-only artifacts under /tmp/. Manual review by Alex before any later phase reuses these PNGs in a public artifact. |
</threat_model>

<verification>
- `ls $AUDIT_DIR/crm/*.png | wc -l` returns at least 8 (the 4 CRM pairs minus events plus the 2 production captures, 8 minimum; 9 with detail variant)
- Wait, recount: dashboard 2 + contacts 2 + contact-detail 2 + alexhollienco 2 = 8 PNGs, plus events-missing.md (1 markdown). Validation matrix expects 8 PNGs and 1 markdown for this plan.
- `test -f $AUDIT_DIR/crm/events-missing.md` returns true
- All PNGs non-zero bytes; `file` confirms PNG magic on each
</verification>

<success_criteria>
8 PNGs + 1 events-missing.md present under $AUDIT_DIR/crm/. Authenticated captures (dashboard, contacts, contact-detail) verified to show CRM chrome rather than the /login form. Production captures (alexhollienco) verified to render the public marketing site. Wave 2 critique can iterate over the captured files.
</success_criteria>

<output>
After completion, write `.planning/phases/023-design-overhaul-crm-audit/023-03-SUMMARY.md` recording each captured PNG (path + byte count), any fallback used for contact-detail, and confirmation that events-missing.md documents the absence per research.
</output>

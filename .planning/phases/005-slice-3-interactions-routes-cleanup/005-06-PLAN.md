---
phase: 005-slice-3-interactions-routes-cleanup
plan: 06
type: execute
wave: 6
depends_on: [005-05]
files_modified:
  - BUILD.md
  - BLOCKERS.md
  - SCHEMA.md
autonomous: true
requirements:
  - SLICE-3-09
  - SLICE-3-10

must_haves:
  truths:
    - "pnpm typecheck exit 0"
    - "pnpm build exit 0"
    - "BUILD.md Built section has Slice 3 entry with date 2026-04-XX"
    - "BLOCKERS.md: 3 Slice-2C-introduced entries moved to Resolved"
    - "BLOCKERS.md: linked_interaction_id audit linkage entry resolved"
    - "SCHEMA.md reflects: tasks.linked_interaction_id added; interactions_legacy dropped; interactions VIEW now activity_events-only"
    - "git tag slice-3-complete present"
    - "PR opened against main"
---

<objective>
Final gate. Run typecheck + build, update repo docs (BUILD.md, BLOCKERS.md,
SCHEMA.md), commit, tag `slice-3-complete`, and open the PR. Spot-check
the runtime via the smoke gates listed below before claiming the slice
shipped.
</objective>

<task_1_gates>
```
cd ~/crm && pnpm typecheck && pnpm build
```

Both must exit 0. If either fails, fix the failure (do not advance).
</task_1_gates>

<task_2_smoke_gates>
Manual smoke (Alex eyeballs in browser):

- Gate A: `/follow-ups` redirects to `/tasks?type=follow_up` (URL bar shows new path).
- Gate B: Tasks page filtered to follow_up shows the post-Slice-2C tasks correctly.
- Gate C: Click "Mark complete" on a follow-up task in `/dashboard` --
  - tasks row updated with status='completed', linked_interaction_id populated
  - activity_events has new row with verb='interaction.{call|email}', context.contact_id set
  - interactions VIEW projects the new row
  - Realtime channel fires (open `/contacts` in another window before clicking)
- Gate D: Submit interaction modal from contact detail page; row appears in activity_events.
- Gate E: POST a captures row with `parsed_intent='interaction'` + matched contact;
  process it; activity_events gets the interaction row (no interactions_legacy INSERT).
- Gate F: `SELECT count(*) FROM pg_tables WHERE tablename='interactions_legacy'` returns 0.
</task_2_smoke_gates>

<task_3_update_docs>
**BUILD.md** -- prepend to `## Built`:

```
- [2026-04-XX] **Slice 3 -- Interactions writeEvent migration + route thinning (plumbing session).**
  Added `tasks.linked_interaction_id` audit-linkage column. Migrated 6 INSERT call sites
  from `interactions_legacy` direct writes to `writeEvent()` (3 server-side callers via
  direct writeEvent; 3 client-side callers via new POST `/api/activity/interaction`
  endpoint that captures the inserted event_id for linked_interaction_id population).
  Backfilled 2 legacy interaction rows into `activity_events` with
  verb='interaction.backfilled' and context.legacy_id idempotency guard. Rewrote the
  `interactions` VIEW to project from `activity_events` only (Part B); dropped
  `interactions_legacy` table CASCADE. Added `activity_events` to `supabase_realtime`
  publication; flipped 2 Realtime subscriptions in `task-list.tsx` and `contacts/page.tsx`
  from `table:'interactions_legacy'` to `table:'activity_events'` with verb filter.
  Deleted `/follow-ups` route; added 308 redirect to `/tasks?type=follow_up`; removed
  sidebar entry; updated command-palette; added Type filter UI + `?type=` query param
  support to `/tasks` page. KEPT `src/components/follow-ups/*` (still used by contact
  detail page Tabs and dashboard quick-actions). Migrations:
  `20260425100000_slice3_tasks_linked_interaction_id.sql`,
  `20260425110000_slice3_legacy_backfill.sql`,
  `20260425120000_slice3_view_rewrite_drop_legacy.sql`. All 11 acceptance gates PASS.
  pnpm typecheck PASS, pnpm build PASS. BLOCKERS.md: 4 entries closed (3 Slice-2C
  interactions_legacy entries + 1 audit-linkage entry).
```

**BLOCKERS.md** -- move these 4 from `## Open` to `## Resolved`:
1. `[2026-04-24] Slice 2C: writers still INSERT directly into interactions_legacy`
2. `[2026-04-24] Slice 2C: interactions_legacy table -- drop deferred to Slice 3`
3. `[2026-04-24] Slice 2C: tasks.completed_via_interaction_id audit linkage lost`
   (resolved by adding `tasks.linked_interaction_id` instead)
4. (none of the others -- /today rebuilds stay open for phase 006)

Each Resolved entry gets the closing date and closing commit hash.

**SCHEMA.md** -- update `tasks` (add `linked_interaction_id uuid FK -> activity_events`),
mark `interactions_legacy` as `dropped`, note `interactions` VIEW now derives from
`activity_events` only.
</task_3_update_docs>

<task_4_commit_tag_pr>
```
cd ~/crm && git add -A
git commit -m "$(cat <<'EOF'
slice 3: interactions writeEvent migration + route thinning

- tasks.linked_interaction_id audit linkage column
- 6 INSERT call sites migrated from interactions_legacy to writeEvent()
- POST /api/activity/interaction endpoint for client-side callers
- 2 legacy rows backfilled into activity_events
- interactions VIEW rewritten (activity_events only); interactions_legacy DROPPED
- activity_events added to supabase_realtime; 2 Realtime channels flipped
- /follow-ups route deleted with 308 redirect to /tasks?type=follow_up
- sidebar + command-palette updated; tasks page gains Type filter

Closes 4 BLOCKERS.md entries.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"

git tag slice-3-complete
git push origin gsd/005-slice-3-interactions-routes-cleanup --tags

gh pr create --title "Slice 3: interactions writeEvent migration + route thinning" --body "$(cat <<'EOF'
## Summary
- tasks.linked_interaction_id added (FK to activity_events)
- 6 interactions_legacy INSERT call sites migrated to writeEvent()
- 2 legacy interaction rows backfilled
- interactions VIEW rewritten; interactions_legacy DROPPED
- /follow-ups route deleted with permanent redirect to /tasks?type=follow_up

## Test plan
- [ ] pnpm typecheck PASS
- [ ] pnpm build PASS
- [ ] /follow-ups -> 308 -> /tasks?type=follow_up
- [ ] /tasks ?type=follow_up renders only follow-up rows
- [ ] Mark-complete on dashboard follow-up writes activity_events row + tasks.linked_interaction_id
- [ ] Interaction modal write lands in activity_events
- [ ] interactions_legacy table absent
- [ ] activity_events in supabase_realtime publication

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```
</task_4_commit_tag_pr>

<verification>
- pnpm typecheck PASS, pnpm build PASS (re-confirmed in Task 1)
- All 6 smoke gates green (Task 2)
- BUILD.md / BLOCKERS.md / SCHEMA.md updated (Task 3)
- git tag slice-3-complete present
- PR URL returned
</verification>

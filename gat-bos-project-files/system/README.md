# System: Routing (live location is canonical)

The classify-first router now lives as ONE file at `~/.claude/rules/routing.md` (flattened
2026-06-04, Scope 5). That live file is canonical. The former portable blueprint in this
folder is archived under `_archived-blueprint-2026-06-04/` -- do not port from it.

`test-matrix.md` (kept here) is the verification harness: run its rows after any routing
change against the live `~/.claude/rules/routing.md`.

# Digital Aesthetic Rules

Shared rules layer for all web, screen, and interactive design outputs.
This file is the **single source of truth** for digital aesthetic direction.
Print skills (`re-print-design`) maintain their own palettes and typography
for physical production. Everything rendered on a screen reads this file.

**Referenced by:** `re-landing-page`, `re-email-design`, `frontend-design` outputs,
any React/HTML artifact, any dashboard or portal UI, GAT-BOS interface components.

---

## Design Philosophy

The target aesthetic is **2028+ digital-native design**. Think: the interface a
senior product designer at a well-funded startup would ship today. Not retro-futurism,
not sci-fi, not gratuitously animated. Instead: precision, depth, confidence, and
small moments of delight that signal craft.

Core principles:

1. **Dark-first, light as variant.** Default to dark themes. Light mode is always
   available but dark is the lead. Dark backgrounds make photography pop, text feel
   sharper, and signal modernity.

2. **Depth over flatness.** Use layered surfaces: frosted glass (backdrop-filter),
   subtle gradients, soft glows, fine borders at 5-8% white opacity. Flat design
   is over. Depth is back, but controlled.

3. **Motion is communication.** Every animation answers a question: "where did that
   come from?" or "what just changed?" Staggered fade-ins on load, smooth height
   transitions on expand/collapse, parallax on scroll. No motion for motion's sake.

4. **Typography carries the hierarchy.** The font pairing does 80% of the design work.
   Display type should feel distinctive. Body type should feel effortless to read.
   Scale contrast between headline and body should be dramatic (3:1 minimum ratio).

5. **Negative space is intentional.** Generous padding signals premium. Dense layouts
   signal utility/dashboard. Choose based on context, never by accident.

6. **Color is surgical.** One or two accent colors max. The rest is neutrals and
   surface tones. Accent color should feel electric against the dark background.

---

## Surface Tier System

All screen outputs belong to one of two surface tiers. The tier determines
which depth effects are allowed.

### Showcase Tier
**Pages:** Login, signup, intake, landing pages, presentations, any public-facing page.

Full depth treatment:
- Glass-effect cards: `backdrop-filter: blur(16px) saturate(120%)` + `border: 1px solid rgba(255,255,255,0.06)`
- Noise texture overlay: SVG feTurbulence at 3% opacity
- Gradient mesh backgrounds: multi-point radial gradients (crimson, blue, purple at 4-8% opacity)
- Layered elevation: multi-layer box-shadows with glow on hover
- Use `showcase-mesh`, `showcase-noise`, `showcase-card` utility classes from globals.css

### Workspace Tier
**Pages:** Dashboard, contacts, tasks, campaigns, follow-ups, materials, tickets,
analytics, actions, opportunities -- all `(app)` routes.

Clean depth treatment:
- Dark color system via CSS variables (`bg-background`, `bg-card`, `text-foreground`)
- Card borders: `border-border` (1px solid, ~16% lightness)
- Hover states: `hover:border-white/[0.12]` (border brightens on hover)
- **No** noise texture, **no** gradient mesh, **no** glass blur, **no** glow shadows
- Dense layouts signal utility. Generous spacing signals premium -- workspace is utility.

### Tier Assignment Rules
- If the page is behind authentication AND is a working tool: **workspace**
- If the page is public-facing OR is an entry point (login, signup): **showcase**
- Landing pages generated by `re-landing-page` skill: **showcase**
- Presentations generated by `re-listing-presentation`: **showcase**
- Email templates: neither tier (email has its own constraints in the Email-Specific Overrides section)

---

## Typography System (Screen Outputs)

### Primary Pairing: Modern Authority

| Role | Font | Weight | Fallback |
|------|------|--------|----------|
| Display / Hero headlines | **Syne** | 700, 800 | 'Inter', system-ui, sans-serif |
| Section headers | **Syne** | 600 | 'Inter', system-ui, sans-serif |
| Body copy | **Inter** | 300, 400, 500 | system-ui, -apple-system, sans-serif |
| Labels / Caps / Metadata | **Inter** | 500, 600 | system-ui, sans-serif |
| Accent / Taglines | **Space Mono** | 400 | 'Courier New', monospace |
| Data / Numbers | **Geist Mono** or **JetBrains Mono** | 400, 500 | monospace |

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=Inter:wght@300;400;500;600;700&family=Space+Mono&display=swap" rel="stylesheet">
```

### Alternate Pairing: Editorial Tech

Use when the output is content-heavy (newsletters, long-form pages, market reports):

| Role | Font | Weight |
|------|------|--------|
| Display | **Cabinet Grotesk** (self-hosted) or **Plus Jakarta Sans** | 700, 800 |
| Body | **Inter** | 300, 400 |
| Accent | **Space Mono** | 400 |

```html
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@300;400;500;600;700&family=Space+Mono&display=swap" rel="stylesheet">
```

### Type Scale (Screen)

```css
--text-hero:    clamp(3rem, 6vw, 5.5rem);    /* 48-88px */
--text-h1:      clamp(2rem, 4vw, 3.5rem);     /* 32-56px */
--text-h2:      clamp(1.5rem, 2.5vw, 2rem);   /* 24-32px */
--text-h3:      1.25rem;                        /* 20px */
--text-body:    1rem;                           /* 16px */
--text-small:   0.875rem;                       /* 14px */
--text-caption:  0.75rem;                        /* 12px */
--text-micro:   0.625rem;                       /* 10px */

--leading-tight:  1.2;
--leading-normal: 1.6;
--leading-loose:  1.85;
```

### Typography Role Assignments (Strict)

Every text element maps to exactly one role. No exceptions.

| Role | Font | Weight | Where It Appears |
|------|------|--------|-----------------|
| Page titles | **Syne** | 700 | `<h1>` on every page, hero headlines on showcase pages |
| Section headers | **Syne** | 600-700 | `<h2>`, `<h3>` section dividers. Never inside cards or list items |
| Card titles | **Inter** | 600 | Card header text, nav item labels, button text |
| Body text | **Inter** | 400 | Paragraphs, descriptions, form labels, list item text |
| Labels / Metadata | **Inter** | 500-600 | Uppercase labels, badge text, category tags |
| Every number | **Space Mono** | 400 | Temperatures, badge counts, dates, prices, stats, pipeline dollar values, issue numbers, bed/bath/sqft, days-ago indicators, percentages, any numeric value displayed to the user |
| Accent / Taglines | **Space Mono** | 400 | Version numbers, codes, editorial asides, step indicators ("Step 01") |

**Syne must never appear:**
- Inside card bodies or list items
- On numeric values
- On button labels or navigation items
- On body text or descriptions

**Space Mono must always appear on:**
- Any rendered number, even if it is a single digit
- Date strings (e.g., "Apr 6", "2026-04-06", "14d ago")
- Dollar values at any size
- Stat displays (beds, baths, sqft, lot size)

**Tailwind class mapping:**
- `font-display` = Syne (via `--font-display` CSS variable)
- `font-sans` = Inter (via `--font-sans` CSS variable, also the body default)
- `font-mono` = Space Mono (via `--font-mono` CSS variable)

### Legacy Typographic Rules (still apply)
- Never use Playfair Display or Montserrat in screen outputs (those are print-only)
- Never use Roboto, Arial, or system defaults as primary fonts

---

## Color System (Screen Outputs)

### Dark Theme (Default)

```css
/* Surfaces */
--surface-base:        #09090b;    /* near-black, slightly warm */
--surface-raised:      #131316;    /* card backgrounds */
--surface-overlay:     #1a1a1f;    /* modals, dropdowns, hover states */
--surface-subtle:      #222228;    /* secondary panels */

/* Borders */
--border-default:      rgba(255, 255, 255, 0.06);
--border-hover:        rgba(255, 255, 255, 0.12);
--border-active:       rgba(255, 255, 255, 0.20);

/* Text */
--text-primary:        #f4f4f5;    /* headings, primary content */
--text-secondary:      #a1a1aa;    /* body, descriptions */
--text-muted:          #71717a;    /* captions, timestamps */
--text-ghost:          #3f3f46;    /* placeholder, disabled */

/* Accent: Electric Crimson (GAT Red evolved for screen) */
--accent-primary:      #e63550;    /* buttons, links, active states */
--accent-hover:        #f04060;    /* hover state */
--accent-subtle:       rgba(230, 53, 80, 0.12);  /* badge bg, tinted surfaces */
--accent-glow:         rgba(230, 53, 80, 0.25);  /* box-shadow glow */

/* Secondary Accent: Deep Signal Blue (GAT Blue evolved for screen) */
--accent-secondary:    #2563eb;
--accent-secondary-subtle: rgba(37, 99, 235, 0.12);

/* Status */
--status-success:      #22c55e;
--status-warning:      #eab308;
--status-error:        #ef4444;
--status-info:         #3b82f6;

/* Gradients */
--gradient-surface:    linear-gradient(135deg, #09090b 0%, #131320 100%);
--gradient-accent:     linear-gradient(135deg, #e63550 0%, #c026d3 100%);
--gradient-glow:       radial-gradient(ellipse at 50% 0%, rgba(230,53,80,0.15) 0%, transparent 60%);
```

### Light Theme (Variant)

```css
--surface-base:        #fafafa;
--surface-raised:      #ffffff;
--surface-overlay:     #ffffff;
--surface-subtle:      #f4f4f5;

--border-default:      rgba(0, 0, 0, 0.06);
--border-hover:        rgba(0, 0, 0, 0.12);

--text-primary:        #09090b;
--text-secondary:      #52525b;
--text-muted:          #a1a1aa;

--accent-primary:      #dc2642;
--accent-subtle:       rgba(220, 38, 66, 0.08);
```

### Brand Color Mapping

| Brand Token | Print Value | Screen Value | Notes |
|-------------|------------|--------------|-------|
| GAT Red | `#b31a35` | `--accent-primary: #e63550` | Boosted saturation + brightness for screen luminance |
| GAT Blue | `#003087` | `--accent-secondary: #2563eb` | Lifted for dark-bg contrast |

Print colors are optimized for CMYK/paper. Screen colors are optimized for backlit
displays. Never use the print hex values on screen; never use screen values in print.

---

## Spacing System

```css
--space-1:   0.25rem;   /* 4px */
--space-2:   0.5rem;    /* 8px */
--space-3:   0.75rem;   /* 12px */
--space-4:   1rem;      /* 16px */
--space-5:   1.5rem;    /* 24px */
--space-6:   2rem;      /* 32px */
--space-8:   3rem;      /* 48px */
--space-10:  4rem;      /* 64px */
--space-12:  5rem;      /* 80px */
--space-16:  8rem;      /* 128px */

/* Section padding */
--section-padding-y:  var(--space-16);     /* generous vertical rhythm */
--section-padding-x:  var(--space-6);      /* horizontal gutter */
--container-max:      1200px;              /* max content width */
--container-narrow:   720px;               /* text-heavy sections */
```

---

## Depth and Surface Effects

### Frosted Glass (Glassmorphism)
```css
.glass-surface {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px) saturate(120%);
  -webkit-backdrop-filter: blur(16px) saturate(120%);
  border: 1px solid var(--border-default);
  border-radius: 12px;
}
```

### Soft Glow Cards
```css
.glow-card {
  background: var(--surface-raised);
  border: 1px solid var(--border-default);
  border-radius: 12px;
  box-shadow:
    0 0 0 1px var(--border-default),
    0 2px 8px rgba(0, 0, 0, 0.3),
    0 8px 32px rgba(0, 0, 0, 0.2);
  transition: border-color 0.2s ease, box-shadow 0.3s ease;
}
.glow-card:hover {
  border-color: var(--border-hover);
  box-shadow:
    0 0 0 1px var(--border-hover),
    0 4px 16px rgba(0, 0, 0, 0.4),
    0 0 40px var(--accent-glow);
}
```

### Noise Texture Overlay
```css
.noise-overlay::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 1;
}
```

### Gradient Mesh Background
```css
.mesh-bg {
  background:
    radial-gradient(ellipse at 20% 50%, rgba(230, 53, 80, 0.08) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 20%, rgba(37, 99, 235, 0.06) 0%, transparent 50%),
    radial-gradient(ellipse at 50% 80%, rgba(192, 38, 211, 0.04) 0%, transparent 50%),
    var(--surface-base);
}
```

---

## Motion and Animation

### Page Load Sequence
```css
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

.animate-in {
  opacity: 0;
  animation: fadeInUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

/* Stagger children */
.stagger > *:nth-child(1) { animation-delay: 0.05s; }
.stagger > *:nth-child(2) { animation-delay: 0.10s; }
.stagger > *:nth-child(3) { animation-delay: 0.15s; }
.stagger > *:nth-child(4) { animation-delay: 0.20s; }
.stagger > *:nth-child(5) { animation-delay: 0.25s; }
.stagger > *:nth-child(6) { animation-delay: 0.30s; }
```

### Scroll-Triggered Reveals
```javascript
// Intersection Observer pattern for scroll reveals
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('animate-in');
      observer.unobserve(entry.target);
    }
  });
}, { threshold: 0.15 });

document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
```

### Hover Micro-interactions
```css
/* Links and buttons */
.interactive {
  transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
}
.interactive:hover {
  transform: translateY(-1px);
}

/* Image cards */
.image-card img {
  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}
.image-card:hover img {
  transform: scale(1.03);
}
```

### Parallax Scroll (CSS-only)
```css
.parallax-container {
  perspective: 1px;
  overflow-y: auto;
  height: 100vh;
}
.parallax-bg {
  transform: translateZ(-1px) scale(2);
  position: absolute;
  inset: 0;
  z-index: -1;
}
```

### Motion Rules
- Easing: always `cubic-bezier(0.16, 1, 0.3, 1)` (spring-like) or `ease-out` for exits
- Duration: 200-400ms for micro-interactions, 500-800ms for reveals, 1000ms+ for hero sequences
- Never animate `width` or `height` directly (use `transform: scale` instead)
- Always include `prefers-reduced-motion` media query:
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### Motion Budget by Tier

**Showcase pages** get the full motion system:
- Page-load stagger: `fadeInUp` with 50ms incremental delays per section
- Scroll-triggered reveals: IntersectionObserver at 15% threshold, one-shot (unobserve after trigger)
- Hover micro-interactions: `translateY(-1px)` on buttons/links, `scale(1.03)` on images, border glow on cards
- Duration budget: 200-400ms for micro-interactions, 500-800ms for reveals
- Carousel/marquee: `linear infinite` for auto-scrolling, `paused` on hover

**Workspace pages** get hover/transition effects ONLY:
- Hover transitions: border-color, background-color, opacity changes
- Duration: under 200ms, ease-out only
- **No** page-load animations, **no** scroll-triggered reveals, **no** staggered fade-ins
- **No** keyframe animations on workspace content
- Sidebar active state transitions are allowed (they are navigation, not content)

**Both tiers require:**
- `prefers-reduced-motion: reduce` media query that kills all animation/transition
- This is already in globals.css -- do not duplicate in component-level styles

---

## Component Patterns (Screen)

### Section Headers
```html
<div class="section-header">
  <span class="section-label">FEATURED LISTINGS</span>
  <h2 class="section-title">This Week's Market</h2>
</div>
```
```css
.section-label {
  font-family: 'Inter', sans-serif;
  font-size: var(--text-caption);
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent-primary);
  display: block;
  margin-bottom: var(--space-3);
}
.section-title {
  font-family: 'Syne', sans-serif;
  font-size: var(--text-h1);
  font-weight: 700;
  color: var(--text-primary);
  line-height: var(--leading-tight);
}
```

### Card Grid
```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: var(--space-5);
}
```

### CTA Buttons
```css
.btn-primary {
  font-family: 'Inter', sans-serif;
  font-size: var(--text-small);
  font-weight: 600;
  letter-spacing: 0.04em;
  padding: var(--space-3) var(--space-6);
  background: var(--accent-primary);
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
}
.btn-primary:hover {
  background: var(--accent-hover);
  transform: translateY(-1px);
  box-shadow: 0 4px 16px var(--accent-glow);
}

.btn-ghost {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-default);
  border-radius: 8px;
  padding: var(--space-3) var(--space-6);
}
.btn-ghost:hover {
  border-color: var(--border-hover);
  background: rgba(255, 255, 255, 0.03);
}
```

### Stat Display (Modern)
```html
<div class="stat-block">
  <span class="stat-value">4,200</span>
  <span class="stat-unit">SQ FT</span>
</div>
```
```css
.stat-block {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-1);
}
.stat-value {
  font-family: 'Geist Mono', 'JetBrains Mono', monospace;
  font-size: var(--text-h2);
  font-weight: 500;
  color: var(--text-primary);
}
.stat-unit {
  font-family: 'Inter', sans-serif;
  font-size: var(--text-micro);
  font-weight: 600;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--text-muted);
}
```

---

## Responsive Breakpoints

```css
/* Mobile first */
--bp-sm:   640px;
--bp-md:   768px;
--bp-lg:   1024px;
--bp-xl:   1280px;
--bp-2xl:  1536px;

@media (min-width: 640px)  { /* sm */ }
@media (min-width: 768px)  { /* md */ }
@media (min-width: 1024px) { /* lg */ }
@media (min-width: 1280px) { /* xl */ }
```

---

## Image Handling (Screen)

```css
.hero-image {
  width: 100%;
  height: 70vh;
  min-height: 500px;
  max-height: 900px;
  object-fit: cover;
  object-position: center;
}

/* Gradient overlay for text legibility on hero images */
.hero-overlay {
  background: linear-gradient(
    to bottom,
    rgba(9, 9, 11, 0) 0%,
    rgba(9, 9, 11, 0.4) 50%,
    rgba(9, 9, 11, 0.95) 100%
  );
}

/* Image reveal on scroll */
.image-reveal {
  clip-path: inset(0 0 100% 0);
  transition: clip-path 0.8s cubic-bezier(0.16, 1, 0.3, 1);
}
.image-reveal.visible {
  clip-path: inset(0 0 0 0);
}
```

---

## Image Treatment: Headshots

**Standing rule:** Never use a hard circle or rectangle crop on any headshot.
Default to a gradient mask that fades the headshot edges into the surrounding
background. The person should appear to float on the layout, not be contained
in a shape.

### Implementation

Use CSS `mask-image` with radial gradients. Three size variants defined in
globals.css:

- `.headshot-mask` -- Standard: agent cards, sidebar, contact detail
- `.headshot-mask-hero` -- Hero: showcase pages, landing page headers, presentations
- `.headshot-mask-sm` -- Small: avatar-sized contexts (< 64px)

### Rules
- If the headshot has a plain studio background, match the canvas color underneath
  and let the figure bleed naturally into the layout.
- Never add `border-radius: 50%` or `rounded-full` to a headshot `<img>` element.
  The gradient mask handles the shape.
- Remove any `ring-*`, `border-*`, or `overflow-hidden` that was previously
  creating a hard crop on headshots.
- The mask center is offset slightly upward (`at 50% 45%` or `at 50% 40%`) to
  keep the face centered even when the fade eats into the bottom.
- For email templates: gradient masks are not supported. Use a `border-radius: 50%`
  circle as fallback -- email is the one exception to this rule.
- For print (PDF via Playwright): CSS mask works in Chromium. Use the same classes.

### Where This Applies
- CRM contact detail page headshots
- Intake page hero headshot and signature headshot
- Login/signup pages (if headshots are added)
- Any agent headshot in dashboard widgets, cards, or lists
- Landing pages generated by `re-landing-page`
- Listing presentations generated by `re-listing-presentation`

---

## Email-Specific Overrides

Email clients strip most modern CSS. For `re-email-design` outputs:

- Use the **dark theme colors** as inline hex values (no CSS variables)
- Use **Inter** for body and **Syne** for display (with fallbacks to Arial/Georgia)
- Simulate glass/depth with background colors and 1px borders (no backdrop-filter)
- Animation is impossible in email; use strong static hierarchy instead
- Table-based layout for Outlook compatibility (this does not change)
- The magazine-editorial feel from the email skill is correct; just apply these
  updated colors and fonts to that existing structure

---

## What This File Does NOT Control

- Print outputs (flyers, brochures, EDDM, door hangers) still use `re-print-design`
  palettes, Playfair + Montserrat + Cormorant Garamond, and print spacing rules
- Agent brand color overrides still apply in both print and screen contexts
- Content structure and section ordering within each skill remain unchanged
- The 3-draft approval process is unchanged
- Fill-and-flag rules are unchanged

# BUILD.md

What we're building. Read at session start alongside `BLOCKERS.md`. Update at session end.

---

## Currently Building

**Slice 8 Phase 5 in flight 2026-05-03 on branch `gsd/021-slice-8-phase-5-cron-registration-dry-run`** (PR pending). PR #21 (Slice 8 Phase 4) merged as `336f0d1`. Three Weekly Edge crons registered in `vercel.json` (altos-pull Mon 13:00 UTC, weekly-edge-assemble Tue 18:00 UTC, weekly-edge-send Tue 20:00 UTC). New `dry-run-alex` recipient list resolver branch (hardcoded to alex@alexhollienco.com) added to `src/lib/campaigns/recipients.ts`. First live dry-run (manual altos-pull → assemble → flip recipient_list_slug → /drafts approve → send) executes after PR merge + prod deploy.

**Next session, recommended: BUILD -- Universal agent welcome email** OR continue Slice 8 follow-ups (Altos credentials provisioning to retire BLOCKERS.md `[2026-05-03]`; subscriber-preferences `recipient_lists` table per LATER.md).

Scope, corrected 2026-04-22: This is NOT an announcement email linking the three live `/agents/<slug>` pages. It is a **single, universal welcome email**, identical for every recipient agent, that showcases the full GAT-BOS resource system -- what Alex offers every agent in his book (design, marketing materials, cadence, systems, handoffs). No other agents' names, no agent-specific incentives, no per-agent landing page links, no listing content. The email is about Alex's system, not about any individual agent. Alex has notes from 2026-04-21 night that spec the resource list to highlight -- load those before planning the three drafts.

Slice 2A plumbing complete (2026-04-23). Migration file at `supabase/migrations/slice-2a-drop-spine.sql` is ready for manual execution in Supabase SQL Editor. Execute it before the next build session to finalize the spine drop.

Capture downstream promotion now wired (Session 6). Captures earn their keep: `/captures` Process button writes real `interactions` / `follow_ups` / `material_requests` rows, links back to the target, idempotent on re-process, guarded when a contact is missing.

---

## Built

- [2026-05-02] **Slice 8 Phase 4 -- Weekly Edge assemble + send + review-gate UI (build session).** Branch `gsd/020-slice-8-phase-4-weekly-edge-assemble-send-review` (off main `cf300d6`). GSD plan at `.planning/phases/020-slice-8-phase-4-weekly-edge-assemble-send-review/PLAN.md`. **Drafts schema gap (option pick): B -- new `campaign_drafts` table.** Rejected Option A (extend `email_drafts` with `kind` column) because that table's `email_id NOT NULL REFERENCES emails(id)` + 30-min `expires_at` + `email_draft_status` enum are reply-shaped invariants; weakening them undermines the Gmail reply path. Rejected Option C as high-risk teardown of working `/drafts` UI. Sibling table is domain-honest: different lifecycle (weekly cadence vs per-email 30 min), different relationships (recipient-list expansion vs single email), different RLS scope (alex-only admin tier vs per-account post-Slice 7B). **New migration** `supabase/migrations/20260503023122_slice8_campaign_drafts.sql` + rollback companion -- table with status text+CHECK constraint (`pending_review`, `approved`, `rejected`, `sent`, `send_failed`), unique partial index `(week_of, template_slug) WHERE deleted_at IS NULL AND status <> 'rejected'`, RLS policies `campaign_drafts_alex_all` (auth) + `campaign_drafts_service_all` (service_role). **Pushed to prod** via `supabase db push --linked` alongside the deferred Phase 1 `weekly_snapshot` migration (both landed clean, NOTICE-skipped on policy DROP IF EXISTS). Types regenerated to `src/lib/supabase/types.ts` (3280 lines; both `campaign_drafts` and `weekly_snapshot` Row/Insert/Update typings present). **6 new ActivityVerb members** in `src/lib/activity/types.ts`: `campaign.draft_created`, `campaign.draft_approved`, `campaign.draft_rejected`, `campaign.draft_edited`, `campaign.sent`, `campaign.send_skipped_unapproved`. **New libs:** `src/lib/campaigns/recipients.ts` (`resolveRecipientList('agents-active')` reads `contacts WHERE type='agent' AND deleted_at IS NULL`); `src/lib/campaigns/render-weekly-edge.ts` (`renderWeeklyEdge({weekOf, issueNumber, markets[]})` resolves max-version `weekly-edge` template, builds opener_html + featured_section_html from writer narrative, leaves PLACEHOLDER bracketed markers in stats_image_html / weekender_image_html / listings_section_html for reviewer fill, emits subject + body_html + body_text + variables map). **Three new routes:** `/api/cron/weekly-edge-assemble` (Bearer `CRON_SECRET`, idempotent on `(week_of, template_slug)` skipping when active draft exists, fetches snapshots per `TRACKED_MARKETS`, runs writer, renders, inserts `campaign_drafts` row status='pending_review', emits `campaign.draft_created`); `/api/cron/weekly-edge-send` (Bearer `CRON_SECRET`, picks oldest approved+unsent draft, expands recipient list, calls `sendMessage()` per recipient with template variables, tracks per-recipient success/failure, updates `sent_at` + `status` + `send_summary{sent, failed, errors[]}`, emits `campaign.sent` per success and `campaign.send_failed` per failure or `campaign.send_skipped_unapproved` when no draft); `/api/campaigns/drafts` (GET lists latest 25, PATCH actions `approve`/`reject`/`edit_html` with state-machine guard restricting mutations to `pending_review`). **UI:** new `src/components/drafts/campaign-drafts-view.tsx` (TanStack Query against `["campaign_drafts","all"]` with 30s stale, status-tone badges, inline `PreviewFrame` from `weekly-edge/preview/preview-frame.tsx`, Approve/Edit HTML/Reject buttons, edit pane is a textarea over `body_html`); `src/app/(app)/drafts/drafts-client.tsx` extended with Reply | Campaign tab strip wired to local state, Reply tab keeps existing implementation untouched. Quality gates: `pnpm typecheck` PASS, `pnpm build` PASS (`/drafts` 16.1 kB up from 15.4 kB; three new API routes registered). **Out of scope, deferred:** `vercel.json` cron registration + first live dry-run (Phase 5); Altos credentials (BLOCKERS.md `[2026-05-03]`); subscriber-preferences / first-class `recipient_lists` table (LATER.md). Bulk approve dropped per plan (single weekly draft = single click). **Pending Alex execution:** push branch + open PR + Alex "ship it" -> automation Phase Completion Protocol squash-merges.

- [2026-05-02] **Slice 8 Phase 3 -- Weekly Edge writer (build session).** Branch `gsd/019-slice-8-phase-3-weekly-edge-writer` (off main `69ec456`). Plan section: `~/.claude/plans/crm-weekly-edge-campaign-infra-2026-04-30.md` Phase 3. New file `src/lib/ai/weekly-edge-writer.ts` mirrors `morning-brief.ts`: `runWeeklyEdgeWriter({snapshot, userId})` resolves the `WeeklySnapshotRow` (Phase 1 schema), short-circuits to a PLACEHOLDER `WeeklyEdgeNarrative` when `snapshot.data.status === 'pending_credentials'` (Phase 2 fallback so the human reviewer rejects in `/drafts` before any send), otherwise calls `callClaude({feature:'weekly-edge-writer', ...})` with system prompt locking the JSON output contract `{headline, market_block, data_callouts[3-5], closing}` and Standing Rule 7 voice. `cacheKey()` derived from prompt_version + week_of + market_slug + narrative_seed + data; 24h TTL. Post-process pipeline `parseJsonObject -> normalizeAndValidate`: tolerates a stray code fence, auto-fixes em + en dashes (U+2014 / U+2013) to ` -- ` per Standing Rule 2 via Unicode-escape regex (literal glyphs forbidden in source by `em-dash-check.sh` hook), throws on banned words (`stunning`, `breathtaking`, `amazing`) and exclamation marks per Rule 7, throws on missing fields, throws on headline > 70 chars, throws when `data_callouts` length is outside `[3, 5]`. New `src/lib/ai/__tests__/weekly-edge-writer.test.ts` (12 tests, all PASS): 4 `runWeeklyEdgeWriter` cases (happy-path JSON + pending_credentials short-circuit + code-fence tolerance + invalid-JSON throw, callClaude mocked via `vi.mock('../_client', ...)`) and 8 `normalizeAndValidate` cases (em-dash auto-fix, en-dash auto-fix, banned-word throw, exclamation throw, headline > 70 chars throw, callouts < 3 throw, callouts > 5 throw, missing field throw). Test fixture covers the realistic Altos shape (`median_price`, `median_dom`, `inventory`, `months_of_supply`, MoM/YoY deltas) so future Phase 4 assembler can reuse it. Quality gates: `pnpm typecheck` PASS, `pnpm test src/lib/ai/__tests__/weekly-edge-writer.test.ts` 12/12 PASS, `pnpm build` PASS. **Out of scope, deferred:** Phase 4 (assembly + send crons + drafts UI extension), Phase 5 (vercel.json cron registration + db push + types regen). **Pending Alex execution:** push branch + open PR + squash-merge.

- [2026-05-02] **Slice 8 Phase 2 -- Altos pull cron + tracked markets (plumbing session).** Branch `gsd/018-slice-8-phase-2-altos-pull`, squash-merged as `69ec456` via PR #19 (`CLAUDE_AUTOMATION_PR_MERGE=19` bypass). New `src/lib/markets/tracked.ts` (TRACKED_MARKETS source of truth, initial seed `scottsdale-85258-sf`); new `src/lib/altos/client.ts` (returns `{status:'pending_credentials'}` until `ALTOS_API_KEY` is provisioned -- fill-and-flag, BLOCKERS.md [2026-05-03] entry); new `src/app/api/cron/altos-pull/route.ts` (Bearer `CRON_SECRET`, service-role upserts on `(week_of, market_slug)` where `week_of` is the Monday of the current ISO week UTC, emits `weekly_snapshot.pulled` / `weekly_snapshot.pull_failed`). Two new ActivityVerb members in `src/lib/activity/types.ts`. Quality gates: `pnpm typecheck` PASS, `pnpm build` PASS. Cron registration deferred to Phase 5; `supabase db push` + types regen still deferred per Alex (gating before prod).

- [2026-05-02] **Slice 8 Phase 1 -- weekly_snapshot table (plumbing session).** Squash-merged as `f1281d9` via PR #18. Migration `supabase/migrations/20260503020437_slice8_weekly_snapshot.sql` creates the table per the plan: `(id, week_of date, market_slug text, market_label text, data jsonb, narrative_seed text, pulled_at, created_at, deleted_at)` with partial unique index on `(week_of, market_slug) WHERE deleted_at IS NULL`. `supabase db push` + types regen deferred per Alex.

- [2026-05-02] **Slice 7C -- Portal Routes + Magic-Link Invite (build session, 14/15 slices live).** Branch `gsd/015-slice-7c-portal-routes`. Plan at `.planning/phases/015-slice-7c-portal-routes-magic-link/PLAN.md`; base `ced5422` (Slice 7B merge). **Task 0** rename `gsd/015-slice-8-weekly-snapshot` -> `gsd/017-slice-8-weekly-snapshot` per OQ#1 (resolved phase-numbering collision; slice-8 branch was local-only with 1 real commit `f663e5c`, 016 already remotely taken by 7A.5). **Task 1** (commit `14a24ca`) `agent_invites` table: PK id uuid; account_id (FK accounts ON DELETE RESTRICT) + contact_id (FK contacts ON DELETE RESTRICT, type='agent'-validated upstream) + token_hash text NOT NULL + expires_at timestamptz default `now() + interval '7 days'` + redeemed_at timestamptz NULL + deleted_at; partial unique index `agent_invites_token_hash_unique WHERE redeemed_at IS NULL AND deleted_at IS NULL` (single-use enforcement); account-scoped RLS via 7B subquery pattern. **Task 2** (`a897ccc`) `redeem_agent_invite(p_token_hash text)` SECURITY DEFINER RPC: validates not-expired AND redeemed_at IS NULL AND deleted_at IS NULL; flips redeemed_at; returns `(email, slug, account_id, contact_id)`; raises P0002 on any unredeemable case (invalid / expired / consumed). GRANT EXECUTE TO anon (route handler invokes via anon client per OQ#3=b). **Task 3** (`68429e0`) `src/lib/auth/requirePortalSession.ts` validates Supabase Auth session + slug-binding (auth.users.email == contact.email AND contact.account_id == slug-resolved account_id); `src/app/portal/layout.tsx` (public portal-scoped shell, Kit Screen tokens, GAT cobrand footer) + `src/app/portal/[slug]/(authed)/layout.tsx` (resolves agent + portal session; redirect to login on no session, 403 on slug/session mismatch). **Tasks 4a-d** (`7056338`/`d7df689`/`f16148f`/`8e8e80a`) four portal pages: dashboard (touchpoints + messages + upcoming events sections, all empty-state pending Slice 7D RPCs), request form + submit action (POST creates `tickets` row scoped to agent's account), events list (attendees JOIN events), messages inbox (read-only `messages_log` entries to agent). **Task 5a** (`3a8342b`) `POST /api/portal/invite`: account-owner-only via `tenantFromRequest` user-kind gate; verifies target contact type='agent' + email + slug present + account match; crypto.randomBytes(32).base64url -> sha256 hex token_hash; INSERT agent_invites under account-owner RLS session; sendMessage() with portal-invite template; returns `{ok, invite_id, expires_at, recipient_email, log_id, redeem_url}` 201; plaintext token only ever in email body and response (not persisted). **Task 5b** (`aa2f5aa`) portal-invite template seed (Kit 1 Instrument Serif + Inter; resend send_mode; variables agent_first_name, agent_slug, redeem_url, portal_url, expires_at_human, expires_at_iso). **Task 5c** (`0bf348e`) `src/app/portal/[slug]/login/page.tsx` "Sending magic link to <email>" UI; outside `(authed)` route group so no portal-session prerequisite. **Task 5d** (`985e4d6` + hex-hoist `e63beea`) `GET /portal/redeem?token=<plaintext>`: sha256-hashes token, calls RPC via anon client, on success calls `admin.auth.admin.generateLink({type:'magiclink', email, options:{redirectTo:/portal/<slug>/dashboard}})`, 302-redirects to action_link (Supabase callback completes OTP exchange + sets session cookie); Kit Screen-styled error HTML for missing_token / invite_unredeemable / rpc_failed / auth_link_failed states. **Task 6** (`afafbde`) `src/middleware.ts` adds `/portal/[slug]/login` + `/portal/redeem` to public bypass; auth-gates everything else under `/portal/[slug]/*`. **Task 7** (`a509fc7`) `scripts/slice7c-portal-smoke.mjs` (~470 lines, 5 layers): Layer 1 schema + RPC defense; Layer 2 anon RLS denial on agent_invites; Layer 3 invite send loop (Julie + Joey, OQ#8=c); Layer 4 redeem flow (regex-extract from messages_log.body_html per OQ#5=a, RPC consume, agent_invites.redeemed_at populated, soft-delete per Rule 3); Layer 5 cross-tenant denial (Julie session blocked from Joey's portal). Layers 1+2 always run; Layer 3 needs dev server on 3000/3001 (Rule 17); Layer 4+5 need SUPABASE_SERVICE_ROLE_KEY + Playwright. **Task 8** quality gates green: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS (all 4 portal pages + redeem route in build manifest), `pnpm test` 101/101 PASS. **Task 9 closed as no-op:** `vercel env ls` confirmed all four env vars Slice 7C touches (NEXT_PUBLIC_SITE_URL, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) already on Production + Preview from prior slices (created 12-13 days ago during 7A/7B). No new envs needed; skipped vercel env add writes; ran `vercel env pull --environment=preview` to refresh `.env.local` (hygiene). **Side-finding from env pull:** `vercel env pull` (no flag) defaults to the Development environment, which on this project has only RESEND_WEBHOOK_SECRET; the no-flag pull silently overwrote 18 vars in `.env.local` -> 2 vars. Recovered via `vercel env pull --environment=preview --yes` (38 vars) + manual restore of `AI_DAILY_BUDGET_USD=5.00` (Slice 6 var that was never pushed to Vercel; lives only in local .env.local). Operational lesson saved to memory + tool-routing-reference: always pass `--environment=<target>` flag explicitly. **Out of scope, deferred to LATER.md:** account_members (multi-user-per-account portal access, per OQ#9); portal mobile-responsive pass; portal mutation surface (agent edits own profile / calendar RSVP / email reply); portal-scoped custom theme deviating from Kit Screen (per OQ#6); HMAC token hardening (per OQ#4); portal SSO (Google/Microsoft); per-agent custom subdomain. **Shipped to main:** PR #16 squash-merged 2026-05-01 as `3628521`; plumbing follow-up `7a72f77` (messages_log.user_id threading + oauth_tokens uuid lookup) landed 2026-05-02. **Smoke 18/0/2 green** against prod; the 2 WARN are an OQ#5 spec mismatch (smoke expected `messages_log.event_sequence[*].payload.body_html`, but `sendMessage` at `src/lib/messaging/send.ts:128-200` renders body_html and dispatches it without persisting it back into event_sequence -- harness falls back to extracting redeem token from response payload, both Julie + Joey redemptions succeed). WARN logged to LATER.md [2026-05-02] for a future smoke harness fix (recommend Option C: flip smoke to validate response payload by design); not a feature bug. **Slice 7C CLOSED.**

- [2026-05-01] **Slice 7B -- Accounts + contacts schema delta + seed agents + public agent read (build session).** Branch `gsd/014-slice-7b-accounts-contacts-seed-agents`. Plan at `~/.claude/plans/2026-04-30-slice-7b-locked.md`; starter at `~/Desktop/slice-starters/slice-7b-starter.md`. **Task 1 contacts schema delta** (commit `4ffc3fc`): added `slug text` (UNIQUE per account via partial index `contacts_account_slug_unique` WHERE deleted_at IS NULL AND slug IS NOT NULL), `tagline text`, `account_id uuid` (FK accounts ON DELETE RESTRICT). `contacts_type_check` extended with `'agent'` and `'escrow'` (the latter a fold-in: 10 prod rows already used `type='escrow'` for escrow officers like Marlene Ruggeri, surfaced by constraint-rebuild row-level revalidation). Q-drift-2 resolved per Standing Rule 16: reused existing `headshot_url` instead of adding `photo_url` (same concept, codebase wins). RLS rewritten to account-scoped via `(SELECT id FROM accounts WHERE owner_user_id = auth.uid() AND deleted_at IS NULL)`. **Task 2 per-table account_id scoping** (atomic per-table commits `2da20eb` tasks, `e615a18` captures, `0c510c4` opportunities, `05f6af5` campaign_enrollments, `cda6e9f` events, `0ad6adc` email_drafts): each table got `account_id uuid` (NOT NULL after backfill from owner_user_id join), backfill row-count assertion, RLS rewrite to account-scoped subquery, FK to accounts ON DELETE RESTRICT. **Task 3 seed 5 agent contact rows** (`3b1114a` original + `4adc707` Path A amendment): Julie Jarmiolowski, Fiona Bigbee, Denise van den Bossche, Joey Gutierrez, Amber Hollien. Path A switched ON CONFLICT from `(account_id, slug)` to `(email)` because four of the five existed in prod as type='realtor' tied to live deals/opportunities/interactions by FK; naive INSERT collided on `contacts_email_unique` before the slug ON CONFLICT could fire. DO UPDATE promotes existing rows in place (overwrites type/slug/tagline/headshot_url/brokerage/title/names; preserves source provenance; doesn't clobber existing phone with NULL). Joey + Amber tagline NULL per Q3=c (route hides null). **Task 4 route refactor** (`80d1417`): `src/app/agents/[slug]/page.tsx` migrated from hardcoded `AGENTS` const to RPC-backed query; 5 SSG slugs prerender. **Task 5 security-definer RPCs** (`0fadd71`): `get_public_agent_slugs()` returns the 5 seeded slugs; `get_public_agent_by_slug(text)` returns 8 whitelisted columns (slug, first_name, last_name, brokerage, title, headshot_url, website_url, tagline) and rejects 31 private columns by harness verification; nonexistent slug returns 0 rows. **Task 6 smoke harness** (`4adc707`): `scripts/slice7b-smoke.mjs` (~340 lines, 3 layers). Layer 1 RPC defense: 7 checks (slug list returns 5, by-slug returns whitelisted only + rejects private columns, nonexistent slug returns 0 rows). Layer 2 anon direct-table denial: 3 checks (anon SELECT contacts/email/phone/account_id/user_id all return 0 rows under RLS). Layer 3 HTTP route smoke: probes 3000/3001 per Standing Rule 17; 5 agent slugs -> 200 + JSON-LD (RealEstateAgent + name + image) + body name + body headshot; nonexistent slug -> 404. Layers 1+2 = 10/10 green against local Docker; Layer 3 deferred to post-prod-deploy gate per A2 audit Path A. **Quality gates:** `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS (`/agents/[slug]` 448 B, 184 kB first-load, 5 SSG paths), `pnpm test` 101/101 PASS. **Production reconciliation (A2 audit Path A):** `supabase migration list --linked` showed 26 pending, not 8 as the plan originally assumed. A2 audit classified each: 17 REPAIR-only (3 mirror migrations + 13 Slice 7A RLS rewrites already paste-applied to prod + 1 local seed) and 9 SAFE-TO-PUSH (the Slice 7B forward migrations, all idempotent by design). Step 1 `supabase migration repair --status applied` for the 17 REPAIR-only IDs (metadata-only write to `supabase_migrations.schema_migrations`, no app DDL). Step 2 confirmed exactly 9 pending (matches Group 3). Step 3 `supabase db push --linked` applied the 9 Slice 7B migrations to prod. Step 4 smoke harness 16/16 green against prod. **Closes BLOCKERS.md [2026-04-21]** contacts table missing slug/photo_url/tagline. **Out of scope, deferred to LATER.md:** Joey/Amber tagline copy resolution; full audit of remaining auth.uid()-only RLS tables not scoped to account_id in 7B (carry to 7C); follow-up on the `'realtor'` vs `'agent'` overlap (Julie is both; flagged for future kind-column or contact_tags migration if it becomes confusing). **Pending Alex execution:** Task 9 push (`git push -u origin gsd/014-slice-7b-...`) to print compare URL.

- [2026-04-30] **Slice 7A -- Multi-tenant auth + RLS rewrite (plumbing session).** Branch `gsd/013-slice-7a-multi-tenant-auth-rls-rewrite`. Backup anchor `20260427-172557` (~880KB Session Pooler dump pre-slice). New `public.accounts` table (single Alex row seeded `name='Alex Hollien' / slug='alex-hollien'`); `tenantFromRequest()` helper at `src/lib/auth/tenantFromRequest.ts` becomes the single resolver for user/account context with explicit failure modes (`no_session`, `no_account`, `invalid_service_token`, `ambiguous_context`); 6 unit tests cover the helper. **15 add-column migrations** added `user_id uuid NOT NULL DEFAULT auth.uid()` + FK to `auth.users` + index across `ai_cache`, `attendees`, `email_drafts`, `emails`, `error_logs`, `event_templates`, `events`, `message_events`, `messages_log`, `morning_briefs`, `projects`, `relationship_health_config`, `relationship_health_scores`, `relationship_health_touchpoint_weights`, `templates` (all backfilled from OWNER_USER_ID `b735d691-4d86-4e31-9fd3-c2257822dca3`). Plus **0b-suppl-16** post-lock structural fix: `oauth_tokens.user_id` text->uuid type conversion (CASE-mapped legacy `'alex'` literal to OWNER_USER_ID), atomic 7-step migration (DROP DEFAULT/UNIQUE -> ALTER TYPE -> SET DEFAULT auth.uid() -> ADD FK -> recreate UNIQUE -> add index). **21 RLS rewrites** (atomic per-policy commits B-1 through B-21): every prior `(auth.jwt() ->> 'email') = 'alex@alexhollienco.com'` policy replaced with column-based `user_id = auth.uid()` USING + WITH CHECK across `ai_cache`, `ai_usage_log`, `attendees`, `email_drafts`, `emails`, `error_logs`, `event_templates`, `events`, `message_events`, `messages_log`, `morning_briefs`, `oauth_tokens`, `project_touchpoints`, `projects`, `relationship_health_config` (R+W), `relationship_health_scores` (R+W), `relationship_health_touchpoint_weights` (R+W), `templates`. Mid-slice smoke gates green after every 3 policy rewrites (#3, #6, #9, #12, #15, #18, #21). Smoke harness at `scripts/slice7a-smoke.mjs` (auth.admin.signInWithPassword fixture `slice7a-smoke@example.test`; user A's positive read of just-inserted row in same harness run is the proof, zero-row reads on user B not load-bearing on their own). **Code cleanup (Phase E, 6 atomic commits):** `writeEvent.ts` hard-break (userId now required, no env fallback); `api-auth.ts` `verifySession` is now tenant-agnostic (ALEX_EMAIL gate dropped); 7 route handlers (`tickets/[id]/actions`, `projects PATCH+POST`, `calendar/{create,sync-in}`, `contacts/{POST,auto-enroll}`, `intake`, `captures/actions`, `email/approve-and-send`) drop OWNER_USER_ID env reads in favor of session/row-derived userId; `ai/_client + _budget` thread userId; drafts UI drops ALEX_EMAIL gate; ALEX_EMAIL constant deleted from `src/lib/constants.ts`. Final greps in `src/`: `OWNER_USER_ID = 0`, `ALEX_EMAIL = 0`. Quality gates: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS. **Out of scope, deferred:** broader `alex@alexhollienco.com` literal cleanup outside test fixtures (UI display, FROM headers, fallback constants -- logged to LATER.md). **Pending Alex execution before final acceptance:** `~/Desktop/PASTE-INTO-ENV-7a-owner-user-id-removal.txt` (remove `OWNER_USER_ID` from `.env.local` + Vercel preview + production); `git push -u origin gsd/013-...`; production smoke against the deployed branch (verify authenticated server client reads work without OWNER_USER_ID).

- [2026-04-27] **Slice 6 -- AI Layer Consolidation + Budget Guard (refactor + schema + env session).** Branch `gsd/012-slice-6-ai-consolidation-budget-guard`. New surface: `src/lib/ai/` namespace consolidating every Claude call site. `_pricing.ts` (PRICING_VERSION=2026-04-27 rate table for Sonnet 4.6 / Haiku 4.5 / Opus 4.6 / Opus 4.7 covering input + output + cache_read + cache_write per 1M; Opus 4.7 rate used as conservative fallback for unknown models). `_budget.ts` (`AI_DAILY_BUDGET_USD` env var, default 5.00, RPC `current_day_ai_spend_usd()` for running spend; `BudgetExceededError` thrown when remaining <= 0; soft-cap warning at 80% writes one `activity_events(verb='ai.budget_warning')` per (feature, Phoenix-day) via in-memory dedupe map; default-used path writes `activity_events(verb='ai.budget_default_used')`). `_cache.ts` (sha256 hex `cacheKey(feature, normalizedInput)` with deterministic stableStringify; `cacheGet`/`cacheSet` against `public.ai_cache`; expires_at NULL = TTL-less; best-effort accessed_at touch on read). `_client.ts` (lazy-init shared Anthropic SDK; default model `claude-sonnet-4-6`; `callClaude({feature, system, messages, model?, max_tokens, tools?, cacheKey?, cacheTtlSeconds?, retryLabel?})` composes cache lookup -> budget check -> retry-wrapped `client.messages.create` -> price + log `ai_usage_log` row -> cache write; failure path also writes a zero-cost log row with error context; cache hit logs cost=0 with `context.cache_hit=true`). Capability files: `morning-brief.ts` (24h TTL keyed by brief_date + ranking digest), `draft-revise.ts` (export `generateDraft` + alias `reviseDraft`; non-AI helpers `wrapReplyHtml`/`detectEscalation` stay in shim), `capture-parse.ts` (Claude structured-JSON intent parser; rule parser stays as guaranteed fallback when AI path fails or returns invalid JSON; sha256 cache key over normalized raw_text + sorted contact-id list, 24h TTL), `inbox-score.ts` (Haiku 4.5 triage scoring routed through callClaude; budget exceeded falls through as no-reply rather than throwing). DB migrations applied via Supabase MCP (Standing Rule 23 paste-file checkpoint waived under explicit Alex authorization; both paste-files archived to `~/Archive/paste-files/2026-04/`): `public.ai_usage_log` (per-call audit + cost; cols feature, model, input/output/cache_read/cache_creation tokens, cost_usd numeric(10,6), occurred_at, context jsonb, user_id, deleted_at; partial indexes on `(occurred_at desc)` and `(feature, occurred_at desc)` WHERE deleted_at IS NULL; Alex-only RLS via auth.jwt) plus SECURITY DEFINER RPC `current_day_ai_spend_usd()` returning numeric SUM scoped to America/Phoenix calendar day. `public.ai_cache` (PK (feature, cache_key), value jsonb, model, expires_at, accessed_at, deleted_at; partial index on (feature, expires_at); Alex-only RLS). Migration mirrors at `supabase/migrations/{20260427250000_slice6_ai_usage_log, 20260427260000_slice6_ai_cache}.sql`. `NOTIFY pgrst, 'reload schema'` issued autonomously after both DDLs. New ActivityVerb values: `ai.budget_blocked`, `ai.budget_warning`, `ai.budget_default_used`. Site migrations (atomic per-site commits): 7a `src/lib/claude/brief-client.ts` -> shim re-exporting `runMorningBrief as assembleBrief`; 7b `src/app/api/captures/route.ts` gains opt-in AI path gated on `process.env.CAPTURES_AI_PARSE === 'true'` (default off, rule parser primary); 7c `src/lib/claude/draft-client.ts` -> shim re-exporting `generateDraft`/`reviseDraft` from ai/draft-revise plus inline non-AI helpers; 7d `src/lib/inbox/scorer.ts` -> shim re-exporting from ai/inbox-score (uncovered during pre-flight 3.e -- not in starter task list). Env var paste-file at `~/Desktop/PASTE-INTO-ENV-slice6-ai-budget.txt` for `AI_DAILY_BUDGET_USD=5.00` across .env.local + Vercel preview + Vercel production. Quality gates: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS. **Pending Alex execution before final acceptance:** set `AI_DAILY_BUDGET_USD` in all three env locations; redeploy; smoke (POST /api/captures with `CAPTURES_AI_PARSE=true` set locally, force budget=0.01 to verify BudgetExceededError + activity_events row, manual RPC call via Supabase MCP, /api/email/approve-and-send revise path still works). **Out of scope, deferred:** flipping `CAPTURES_AI_PARSE=true` after 7-day soak; deleting brief-client.ts + draft-client.ts + inbox/scorer.ts shims in Slice 7; migrating `/api/email/approve-and-send` send path to `sendMessage()` (still LATER.md from Slice 4).

- [2026-04-27] **Slice 5B -- Post-creation event hooks + weeklyWhere + touchpoint-reminder cron (plumbing session).** Branch `gsd/011-slice-5b-event-hooks-touchpoints`. New `src/lib/hooks/` namespace: `post-creation.ts` dispatcher with named-handler array per kind (project | contact | event); each handler isolated in try/catch, failures logged to `error_logs` + `activity_events(verb='hook.failed')` so one handler cannot cascade. Three handlers landed: `handlers/project-created.ts` (idempotent on `activity_events(verb='project.hook_fired')`; listing path inserts 1 `project_touchpoints` row with `touchpoint_type='listing_setup'` + 3 launch tasks + 2 proactive `email_drafts` via synthetic `emails` parent rows keyed on `gmail_id='proactive-listing-launch-{projectId}-{slug}'` for idempotency; non-listing path inserts 1 generic `contact_note` touchpoint + 3 generic tasks). `handlers/contact-created.ts` welcome task (idempotent on `(contact_id, source='contact_hook')`, type='follow_up', status='open', due+3d, action_hint references `/agent-creative-brief`). `handlers/contact-auto-enroll.ts` thin adapter wrapping `autoEnrollNewAgent` so the dispatcher can run it as an isolated handler in parallel with the welcome task. `handlers/event-created.ts` (idempotent on `(entity_table='events', entity_id)`; project_id non-null path inserts `project_touchpoints(touchpoint_type='event')`; project_id null + contact_id non-null path writes `activity_events(verb='event.contact_only')` instead of a touchpoint per G6 resolution). New `src/lib/touchpoints/weeklyWhere.ts` returns `{ endIso, asOfIso, asOfDayOfWeek }` for the upper-bound timestamp marking end-of-Sunday in America/Phoenix; computes target-Sunday calendar date first then runs end-of-day in that day's zone state so DST transitions never produce a 1-hour drift; `asOf` injection for tests; 6 unit tests passing covering mid-week, Sunday-night straddle, DST forward + back under America/Denver, default no-asOf, and cross-zone determinism. New `src/app/api/cron/touchpoint-reminder/route.ts` (Bearer CRON_SECRET via `verifyCronSecret`; queries touchpoints + tasks via `weeklyWhere().endIso`; `last_reminded_at < now() - 20h` debounce on touchpoints; cap 50 surfaced rows + "Plus N more" footer; renders subject + body via `sendMessage` resend mode against `daily-touchpoint-summary` template; updates `last_reminded_at` on surfaced touchpoint rows; tasks have no debounce column so same-day re-runs may include the same task -- acceptable for v1 single 5am tick). Wire-ins: `firePostCreationHooks` dispatched from `src/app/api/projects/route.ts` (Task 1), `src/app/api/contacts/route.ts` (replaces direct `autoEnrollNewAgent`), `src/lib/intake/process.ts` (replaces direct `autoEnrollNewAgent`), `src/app/api/contacts/[id]/auto-enroll/route.ts` (replaces direct `autoEnrollNewAgent`; endpoint now returns `{ok: true}` because handler outcomes vary per-isolation), `src/app/api/calendar/create/route.ts` (after `writeEvent('event.created')`), and `src/app/api/calendar/sync-in/route.ts` (per-row inside `upsertEvent`). New ActivityVerb values: `project.hook_fired`, `contact.hook_fired`, `event.hook_fired`, `event.contact_only`, `hook.failed`. New cron entry in `vercel.json`: `{"path":"/api/cron/touchpoint-reminder","schedule":"0 12 * * *"}` (5:00 AM MST, fires 30 min before morning-brief). **Schema deltas (consolidated, idempotent, executed via paste-file Task 7b)**: `project_touchpoints` gained `due_at`, `deleted_at`, `user_id` NOT NULL (backfilled to OWNER_USER_ID `b735d691-4d86-4e31-9fd3-c2257822dca3`), `last_reminded_at`; partial indexes `idx_project_touchpoints_due_at` + `idx_project_touchpoints_last_reminded_at` WHERE `deleted_at IS NULL`; `tasks.project_id` FK to `projects(id) ON DELETE SET NULL` + partial index; enum `project_touchpoint_type` gained value `'listing_setup'` (G2 path-a). Migration mirrors at `supabase/migrations/{20260427220000_slice5b_schema_deltas, 20260427230000_slice5b_listing_templates, 20260427240000_slice5b_daily_summary_template}.sql`. Three template seeds in live DB (executed via paste-files): `listing-launch-invite` v1 (gmail send_mode), `listing-launch-social` v1 (gmail), `daily-touchpoint-summary` v1 (resend send_mode, Kit 1 Instrument Serif + Inter, expects variables `{date, count_total, rows_html, rows_text, overflow_count}`). Decisions executed: (D1) G2 enum path-a `listing_setup` shipped; (D2) cron schedule `0 12 * * *` confirmed; (D3) `tasks.project_id` added now; (D4) enum DDL kept inside 7b -> 3 migration files total. Mid-execution divergences from plan: (a) Task 3 plan called for "2 email_drafts via templateSlugs" but `email_drafts.email_id` is NOT NULL FK to `emails` and `emails.gmail_id` is UNIQUE NOT NULL with no default; resolved per Alex decision (synthetic emails row pattern with sentinel `gmail_id` for idempotency); now an established convention. (b) tasks check constraint allowed `('open','done','snoozed','cancelled')` not `('pending','completed')`; switched welcome + project tasks to `status='open'` and the cron filter to `NOT IN ('done','cancelled')`. (c) `emails.created_at` NOT NULL with no default forced explicit `created_at` injection in synthetic upsert. Quality gates: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings; touchpoint-reminder route file carries a scoped `eslint-disable no-restricted-syntax` for inline email hex + HTML numeric character references that legitimately ship in template output and would otherwise trip the brand-token banner-rule), `pnpm build` PASS, `pnpm test` 95/95 PASS (6 new weeklyWhere cases). **Smoke test green end-to-end** against locally-running dev server on port 3001: Gate 1 contact create -> 1 welcome task + 1 enrollment; Gate 2 re-fire `/api/contacts/[id]/auto-enroll` -> still 1 task (idempotency); Gate 3 listing project -> 1 touchpoint + 3 tasks + 2 drafts (via 2 synthetic emails parents); Gate 4 cron tick -> 5 surfaced items, summary email queued (logId returned), `last_reminded_at` populated on surfaced touchpoint; Gate 5 soft-delete project + touchpoints + tasks + re-tick cron -> deleted-project rows correctly excluded. All test fixtures soft-deleted per Standing Rule 3. Smoke runner at `~/crm/scripts/slice5b-smoke.mjs`. **Out of scope, deferred:** Migrating `/today` reads to `weeklyWhere()` (Slice 6+, logged to LATER.md). **Pending Alex execution before final acceptance:** push branch + open PR + production-side smoke (curl `https://gat-bos.vercel.app/api/cron/touchpoint-reminder` with prod CRON_SECRET, verify Resend delivery to `alex@alexhollienco.com`).

- [2026-04-27] **Slice 5A -- Campaign runner cron + drip enrollments + Resend webhook -> message_events (plumbing session).** Branch `gsd/010-slice-5a-campaign-runner-enrollments`. New / changed surface: `src/app/api/cron/campaign-runner/route.ts` (15-minute cron tick, Bearer CRON_SECRET, LIMIT 50 per tick, scheduling re-keyed to absolute-from-`enrolled_at` so step.delay_days reads as "fire this step at enrolled_at + delay_days"), `src/app/api/webhooks/resend/route.ts` extended to insert `public.message_events` rows after HMAC verify (Slice 5A Task 2 status-sync trigger advances `messages_log.status`), `src/lib/campaigns/actions.ts:autoEnrollNewAgent()` fixed (current_step=0 instead of 1 -- prior value was silently skipping step 1; user_id passed explicitly because all 3 callers use service-role adminClient where auth.uid() is NULL). New tables: `public.message_events` (Slice 5A Task 2: FK messages_log ON DELETE CASCADE, event_type enum sent/delivered/opened/clicked/bounced/complained, payload jsonb, AFTER INSERT trigger `update_message_log_status()`, Alex-only RLS). New columns: `campaign_steps.template_slug text NULL` + partial index `idx_campaign_steps_active_with_slug` (Task 4); `campaign_enrollments.next_action_at` + partial indexes were already on the books from the [2026-04-22] paste-file (Task 1 was a no-op confirmation). Drip content: 4 New Agent Onboarding templates seeded (slugs `new-agent-onboarding-step-1`..`-4`, kind='campaign', send_mode='gmail', body_html + body_text imported from existing inline copy on the campaign step rows; subject text preserved verbatim per "preserve verbatim agent source" memory feedback); 4 existing campaign step rows backfilled with template_slug. Plus Agent Nurture: 2 templates + 1 campaign (id `85af274e-ae78-4a32-9915-fefb952dda43`) + 2 steps (Day 0 monthly recap + Day 30 soft re-engage; both copy bodies carry `[PLACEHOLDER]` rotating-recap and city-name tokens per Standing Rule 1 fill-and-flag). vercel.json carries `{ "path": "/api/cron/campaign-runner", "schedule": "*/15 * * * *" }` from Task 3. Migration files registered in MCP registry (Rule 23 suspended for this slice per Alex authorization, MCP `apply_migration` was the DDL channel; local migration files mirrored Tasks 1 + 2 only since those were applied via paste-file before the rule change; Tasks 4, 5, 6 ran straight through MCP and follow Slice 4 precedent of no local duplicate). Quality gates: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS (`/api/cron/campaign-runner` route in build manifest, /campaigns 458 B / /campaigns/[id] 8.87 kB). Smoke (live, against `localhost:3000`): cron route reachable; empty queue returns `{processed:0}`; due enrollment with NULL contact email returns `{processed:1, skipped:1}`, advances current_step 0 -> 1, schedules next_action_at = enrolled_at + 3 days (matches step 2 delay_days), records `activity_events` row `verb='campaign.step_skipped' reason='contact_email_missing'`. Test fixtures soft-deleted per Standing Rule 3 (contact `11111111-...`, enrollment `22222222-...`). **Out of scope, deferred (Slice 5B + later):** post-creation event hooks (invite draft, social draft, 3 tasks); weeklyWhere.ts; nightly touchpoint-reminder cron; AI send-time optimization; daily AI budget cap; migrating `/api/email/approve-and-send` to call `sendMessage()` (logged to LATER.md); exponential backoff on send failures; multi-account send queueing; Gmail bulk-send rate-limit awareness; campaign UI editor; step-completion analytics; subscriber/unsubscribe management. **Pending Alex execution before final acceptance:** real send smoke (test contact with valid email -> trigger cron -> observe message land in inbox), Resend dashboard webhook fan-out test (fire `email.delivered` against a known provider_message_id, verify `message_events` row + `messages_log.status` flip).

- [2026-04-27] **Migration registry reconciliation (plumbing session).** Closed Stream A priority 1 from `~/crm/.plan/2026-04-27.md`. Synthesis flagged 11 unsynced local migrations; deeper audit found 19 (including pre-Slice-2 `api_usage_log` + `inbox_items`). Plan at `~/.claude/plans/2026-04-27-migration-registry-reconciliation.md`. Pre-existing remote-vs-local timestamp drift broken: re-stamped `20260407021000_spine_interactions_trigger.sql` -> `20260407021500` and `20260408001000_cleanup_spine_smoke_test_data.sql` -> `20260408001500` to clear collisions with the registered `dashboard_pieces_*_placeholder` rows on those timestamps. Deleted redundant local Slice 4 files (`20260427120000_slice4_templates.sql`, `20260427130000_slice4_messages_log.sql`) -- registry already carried equivalents at `20260427155808` / `20260427155918` from MCP `apply_migration` auto-stamping during Slice 4. 18 unregistered local versions registered via `supabase migration repair --status applied --yes <version>` (CLI invoked directly from this session under explicit Alex authorization; Standing Rule 23 paste-file checkpoint waived for this run because the registry write is registry-only, not schema mutation). Final state: `supabase_migrations.schema_migrations` row count 12 -> 30, verified via MCP `execute_sql`; `supabase migration list` shows 26 matched local+remote pairs and 4 remote-only Slice 4 entries (`20260427155808/155918/160946/161549`) whose local files were intentionally deleted. `pnpm typecheck` PASS, `pnpm build` PASS (route table unchanged). Paste-files archived: `~/Desktop/PASTE-INTO-SUPABASE-migration-repair.sql` and `~/Desktop/PASTE-INTO-TERMINAL-migration-repair.sh` -> `~/Archive/paste-files/2026-04/` per Rule 22. **Side-finding:** the .sh paste-file crashes on macOS bash 3.2 due to `set -u` + empty-array `${PASSWORD_FLAG[@]}` expansion; ran the repair loop inline as a workaround. Scripts that target macOS default bash should avoid `set -u` with optional arrays. **Deferred to BLOCKERS.md (new entry):** 6 non-conforming filenames (`phase-1.3.1-gmail-mvp.sql`, `phase-1.3.2-observation.sql`, `phase-1.4-projects.sql`, `phase-1.5-calendar.sql`, `phase-9-realtime-email-drafts.sql`, `slice-2a-drop-spine.sql`) silently skipped by the CLI -- registry hygiene only, not data risk.

- [2026-04-27] **Slice 4 -- Templates + messaging abstraction (plumbing session).** Branch `gsd/009-slice-4-templates-messaging-abstraction`. Net-new tables: `public.templates` (versioned by `(slug, version)` unique index, partial index on live rows; `template_send_mode` enum `resend`/`gmail`/`both`; `template_kind` enum `transactional`/`campaign`/`newsletter`; `updated_at` trigger; Alex-only RLS) and `public.messages_log` (FK to `templates` ON DELETE RESTRICT; `message_status` enum `queued`/`sent`/`delivered`/`bounced`/`opened`/`clicked`/`failed`; `event_sequence` jsonb append-only mirroring `email_drafts.audit_log` shape; indexes on `(template_id, sent_at desc)` and partial `(status, created_at desc)` WHERE `deleted_at IS NULL`; Alex-only RLS). New `src/lib/messaging/` namespace: `send.ts` orchestrates resolve-by-slug + Handlebars-lite render + dispatch + lifecycle logging; `types.ts` (TemplateRow/MessageLogRow/SendMessageInput/SendMessageResult); `render.ts` (regex `{{ key }}` replacer with `unresolved[]` surfaced); `adapters/resend.ts` (sole `RESEND_API_KEY` reader inside the namespace; honors `RESEND_SAFE_RECIPIENT`); `adapters/gmail.ts` (uses `loadTokens()` + `getOAuth2Client()` from `oauth.ts`; multipart/alternative RFC 5322; `gmail.users.messages.send` with `userId='me'`). Mode resolution: explicit `mode` arg overrides `template.send_mode`; `both` routes live send through Gmail and additionally fires Resend for instrumentation (Resend failures non-fatal on the `both` path). Weekly Edge HTML seeded from the canonical `~/.claude/skills/re-email-design/evals/iteration-1/weekly-edge-full` eval-output (Q1 decision); variableized into 8 locked variables (Q3 decision): `issue_number`, `issue_date_long`, `issue_date_metadata`, `opener_html`, `stats_image_html`, `weekender_image_html`, `featured_section_html`, `listings_section_html`. Subject template reuses `issue_number` + `issue_date_long`. Note: eval-output ships with Syne + Playfair Display + Inter + Space Mono (newsletter-aesthetic screen-tier pairing) -- diverges from brand.md Email = Kit 1 (Instrument Serif + Inter); preserved verbatim per Q1, font conformance reconciliation logged to LATER.md. Carryforward A: `src/lib/gmail/oauth.ts:getStateSigningKey()` fallback to `OAUTH_ENCRYPTION_KEY` removed (one-slice fallback retired; deploy gap >> 10-min state-nonce TTL). `OAUTH_ENCRYPTION_KEY` remains in service in `src/lib/crypto/vault.ts` for at-rest token encryption (separate purpose from state signing). Carryforward B: `/api/inbox/scan` migrated to `src/lib/gmail/sync-client.ts:fetchUnreadThreads()` (oauth_tokens-backed); legacy `src/lib/gmail/client.ts` deleted; `GOOGLE_REFRESH_TOKEN` removal instructions written to `~/Desktop/PASTE-INTO-ENV-slice4-google-refresh-token-removal.txt`. Carryforward C: `_deprecated_requests` hard-removed CASCADE after gate (both pre-condition counts = 0); SCHEMA.md row removed; `activities.request_id` column remains (0 non-null rows; explicit column drop deferred). Migration files (4): `20260427120000_slice4_templates.sql`, `20260427130000_slice4_messages_log.sql`, `20260427140000_slice4_weekly_edge_template_seed.sql`, `20260427150000_slice4_drop_deprecated_requests.sql`. Quality gates: `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS. **Mid-slice incident:** Task 8 commit (`add681b`) inadvertently deleted three Slice 4 SQL migration files; recovered via hotfix commit `57a6c99` (`git checkout HEAD~1 -- ...`); root cause not fully diagnosed, suspect PostToolUse hook side-effect interacted with the `rm` for `client.ts`. Also flagged: same commit unintentionally renamed two pre-existing spine migrations (`20260407021000` -> `021500` and `20260408001000` -> `001500`); not reverted in-session, follow-up audit before PR merge. **Out of scope, deferred to Slice 5A:** campaign runner cron, campaign-driven enrollments, Resend webhook open/click/bounce ingestion into messages_log. **Deferred to LATER.md:** migrating `/api/email/approve-and-send` to call `sendMessage()`; explicit drop of `activities.request_id` column; Weekly Edge font-stack reconciliation. **Pending Alex execution before final acceptance:** smoke test (curl `/api/inbox/scan`, OAuth re-authorize flow), `GOOGLE_REFRESH_TOKEN` env removal across `.env.local` + Vercel preview + production + `.env.example`, audit of spine-migration timestamp drift.

- [2026-04-26] **Phase 008 -- /today-v2 surface (live-bind ratification, read-only).** Branch `gsd/008-today-v2-live-bind`, commit `cb047c3`. Ratifies ~2130 lines of off-protocol today-v2 work that landed untracked: `src/app/(app)/today-v2/{page.tsx, today-v2-client.tsx (525), queries.ts (596), styles.module.css (940), fixtures.ts}`. UI: StatusBar, CallsLane, Runway, ListingActivity, GCalendar, Moments. Five live React Query hooks (`useCallsLane`, `useListings`, `useMoments`, `useRunway`, `useStatusBarStats`) hit Supabase read-only with 30s staleTime. Realtime subscription scoped to `email_drafts` only (latency-sensitive); other lanes rely on staleness tolerance. Decision 1a applied: fonts swapped from Instrument Serif + JetBrains Mono to Kit Screen (Syne + Inter + Space Mono); page.tsx no longer loads next/font locally, styles.module.css `--font-serif` rebinds to inherited `--font-display`. Decision 2a applied: `CADENCE` map (queries.ts:31) stays inlined; canonical source `src/lib/scoring/temperature.ts` parked behind `dddc0b0` on `gsd/006-...` until post-Slice-4 merge (LATER.md follow-up logged). Decision 3a applied: empty `today-v2/components/` directory removed. Hygiene commit `3efa0c0` (`chore(008-task-0a)`) added `.playwright-cli/` to `.gitignore`. **Out of scope, deferred to Phase 009:** all mutations (runway toggle persistence, listing check-off, moments snooze, calls-lane "mark called"), Realtime expansion beyond `email_drafts`, /today -> /today-v2 cutover, mobile responsive pass.

- [2026-04-26] **Out-of-pattern QA fix landed directly on main** (commit `b58e998`). Hid soft-deleted opportunities from list views and stubbed `agent_health` queries that were returning errors. Shipped without a slice plan or feature branch. Documented retroactively for audit trail. Future similar fixes should still go through a hygiene branch even when scoped small.

- [2026-04-26] **Slice 3B -- Ticket unification + OAuth state-signing decouple + lib carryforwards (plumbing session).** DB renamed `material_requests`→`tickets` (23 rows), `material_request_items`→`ticket_items` (23 rows; FK renamed to `ticket_items_ticket_id_fkey`), and orphan `requests`→`_deprecated_requests` (0 rows, soft-deprecate per Standing Rule 3; eventual hard-drop tracked in LATER.md). Indexes (`idx_material_requests_*`→`idx_tickets_*`), trigger (`material_requests_updated_at`→`tickets_updated_at`), and RLS policies cosmetically renamed in the same paste. PostgREST schema cache reloaded via MCP `NOTIFY pgrst, 'reload schema'`. Source code: 21 `.from('material_requests')` sites across 11 files swapped to `tickets`; 2 `material_request_items(*)` join projections + corresponding `r.material_request_items` aliases swapped to `ticket_items`; `writeEvent.object.table` literals in `tickets/[id]/actions.ts` flipped from `'material_requests'` to `'tickets'`. Stale comment refs in `src/lib/contact-activity.ts` + `src/lib/intake/process.ts` updated for grep-cleanliness. Route surface collapsed: `src/app/(app)/materials/page.tsx` reduced to a `permanentRedirect("/tickets")` 308 redirect (148 B route bundle); intake badge + Weekly Edge preview link ported into `tickets/page.tsx` PageHeader right slot alongside existing pending/this-week/avg metrics. Sidebar consolidated from dual `Materials`+`Tickets` entries to one `Tickets` entry (Printer icon retained); command palette `Materials`→`Tickets`; capture promote-targetUrl branches swapped to `/tickets`. OAuth state signing decoupled from `OAUTH_ENCRYPTION_KEY` via new `OAUTH_STATE_SIGNING_KEY` env var with one-slice fallback to the old key (10-min state-nonce TTL is the boundary; LATER.md tracks fallback removal). Lib carryforwards from Slice 3A landed: `src/lib/captures/parse.ts`→`rules.ts` (3 import sites updated; plan listed 2 but `capture-bar-server.tsx` was a third); `src/lib/captures/promote.ts` folded into `actions.ts`; `src/lib/campaigns/auto-enroll.ts` folded into `actions.ts` (3 callers); `src/lib/events/invite-templates/` (8 sub-files) promoted to single-file `invite-templates.ts` with two consolidations (4 private `buildPlainText` helpers renamed per renderer; 3 identical `firstName` helpers collapsed to one shared). Acceptance gates green: `grep -rn "material_requests\|material_request_items" src/` returns zero hits; `grep -rn "/materials" src/` returns only `@/components/materials/*` import paths (deferred per LATER.md); `pnpm typecheck` PASS, `pnpm lint` PASS ("No ESLint warnings or errors"), `pnpm build` PASS (`/tickets` 6.91 kB, `/materials` 148 B). 8 commits on branch `gsd/007-slice-3b-ticket-unification-oauth-cleanup-lib-carryforward`. **Pending Alex execution before merge:** add `OAUTH_STATE_SIGNING_KEY` from `~/Desktop/PASTE-INTO-ENV-slice3b-oauth-state-signing-key.txt` to `.env.local` + Vercel preview + production. **Deferred to LATER.md:** hard-drop `_deprecated_requests` after 30-day soak; remove `OAUTH_ENCRYPTION_KEY` fallback after one slice cycle; per-provider OAuth keys when non-Google providers arrive; `@/components/materials/*` directory rename to `@/components/tickets/*`; `MaterialRequest*` type identifiers rename to `Ticket*`; remaining FK constraint cosmetic rename; `/api/inbox/scan` migration to `oauth_tokens`-backed sync client (also added to BLOCKERS.md as Slice 4 follow-up).

- [2026-04-24] **Slice 3A -- Route thinning + lib shape standardization + Supabase-backed rate limiter (plumbing session).** Tasks 1+2 (extract draftActions + intake orchestration to pure helpers under src/lib) shipped earlier in the session. Task 3 added `public.rate_limits` operational table (PK `(key, window_start)`, RLS deny-all for anon/authenticated, service-role only) + `increment_rate_limit(p_key, p_window_start)` SECURITY DEFINER RPC for atomic INSERT...ON CONFLICT DO UPDATE SET count=count+1 RETURNING count. Helper at `src/lib/rate-limit/check.ts` exposes `checkRateLimit(key, limit, windowSec)` returning `{allowed, remaining, resetAt}`; lazy cleanup of rows older than 2x the longest window; fail-open with console.warn on any Supabase error (availability over enforcement on inbound writes). IP extraction at `src/lib/rate-limit/extract-ip.ts` (first-hop x-forwarded-for, fallback x-real-ip, fallback "unknown"). Task 4 wired limits: `/api/intake` 10/5min, `/api/captures` 30/60s, `/api/captures/[id]/process` 20/60s, all per IP, all return `{error:"rate_limited", retryAfter}` + `Retry-After` header on 429. `/api/webhooks/resend` deliberately UNLIMITED with leading comment documenting why (Svix HMAC IS the boundary; rate-limiting webhook bursts would drop legitimate Resend events during normal email activity). Task 5 standardized 8 `src/lib/<entity>/` dirs (projects, tickets, tasks, contacts, opportunities new from scratch; captures, events, campaigns added missing files alongside existing parse/promote/auto-enroll/invite-templates) to actions.ts/queries.ts/types.ts shape; Row/Insert/Update types stubbed; future slices fill bodies. 4 follow-ups logged to BLOCKERS.md as Slice 3B work. Migration files: `supabase/migrations/20260425140000_rate_limits.sql` (table) + `supabase/migrations/20260425150000_rate_limits_rpc.sql` (RPC). Vitest 89/89 PASS (15 new tests for rate-limit window math, increment, allowed/blocked transitions, fail-open paths, and extract-ip header parsing). pnpm typecheck PASS, pnpm lint PASS (zero warnings), pnpm build PASS. Tag `slice-3a-complete`. **Pending Alex execution:** the table paste (`PASTE-INTO-SUPABASE-rate-limits.sql`) was executed and archived during the session; the RPC paste (`~/Desktop/PASTE-INTO-SUPABASE-rate-limit-rpc.sql`) is still on Desktop. Until that paste lands, the helper fails open on every call (visible via `[rate-limit] check failed, failing open` in Vercel logs); response parity is preserved, no requests are blocked, no rate-limit errors are emitted. Live smoke (`pnpm dev` + 11x `/api/intake` POST to confirm 429 on the 11th) deferred until the RPC paste is executed; once executed, archive the file to `~/Archive/paste-files/2026-04/` per Standing Rule 22 and run the smoke against `gsd/006-slice-3a-route-thinning-lib-standardization`.

- [2026-04-24] **Slice 3 -- Interactions writeEvent migration + route thinning (plumbing session).** Added `tasks.linked_interaction_id uuid` audit-linkage column with FK to `activity_events.id` and partial index. Migrated 6 INSERT call sites from `interactions_legacy` direct writes to `writeEvent()`: 3 server-side callers (`promote.ts`, `intake/route.ts`, `webhooks/resend/route.ts`) call writeEvent directly; 3 client-side callers (`actions/page.tsx`, `task-list.tsx` 2x mutations, `interaction-modal.tsx`) call new POST `/api/activity/interaction` endpoint that captures the inserted event_id for `tasks.linked_interaction_id` population on follow-up completion. Extended `ActivityVerb` union with 10 `interaction.*` verbs matching `interaction_type` enum. Backfilled the 2 `interactions_legacy` rows into `activity_events` with `verb='interaction.backfilled'` + `context.legacy_id` idempotency guard + `context.type` to preserve original interaction_type for VIEW projection. Rewrote `interactions` VIEW to drop Part A; VIEW now projects only from `activity_events WHERE verb LIKE 'interaction.%'` with `COALESCE(context->>'type', REPLACE(verb, 'interaction.', ''))` to preserve type for backfilled rows. DROPPED `interactions_legacy` CASCADE. Added `activity_events` to `supabase_realtime` publication. Flipped 2 Realtime subscriptions (`task-list.tsx:374` + `contacts/page.tsx:145`) from `table:'interactions_legacy'` to `table:'activity_events'` with verb-prefix filter inside the callback. Deleted `/follow-ups` route + directory; added 308 redirect `/follow-ups -> /tasks?type=follow_up` in `next.config.js`; removed `Follow-ups` sidebar entry + `Clock` import; updated command-palette navigate target. `/tasks` page split into `page.tsx` (Suspense wrapper) + `tasks-client.tsx` (useSearchParams body); accepts `?type=` query param; new Type filter Select (All/Todo/Follow-up/Commitment) sits next to existing status filter. Kept `src/components/follow-ups/*` -- still used by contact detail page Tabs and dashboard quick-actions; nothing depends on the deleted route. Migration files: `20260425100000_slice3_tasks_linked_interaction_id.sql`, `20260425110000_slice3_legacy_backfill.sql`, `20260425120000_slice3_view_rewrite_drop_legacy.sql` -- all written; consolidated paste at `~/Desktop/PASTE-INTO-SUPABASE-slice3.sql` for Alex to execute via Supabase SQL Editor (CLI deferred due to pre-existing local-vs-remote migration mismatch). pnpm typecheck PASS, pnpm build PASS. BLOCKERS.md: 3 Slice-2C-introduced entries closed (writers on writeEvent, interactions_legacy dropped, audit linkage restored).

- [2026-04-24] **Slice 2C -- Tasks merge, opportunities consolidation, interactions view (plumbing session).** Extended tasks table with type/source/due_reason/action_hint columns and tasks_type_check constraint (CHECK type IN ('todo','follow_up','commitment')). Merged follow_ups rows (0) into tasks with type='follow_up'; dropped follow_ups table. Added 13 deal-specific columns to opportunities (buyer_name, seller_name, earnest_money, commission_rate, escrow_company, escrow_officer, title_company, lender_name, lender_partner_id, contract_date, escrow_open_date, scheduled_close_date, actual_close_date); merged deals rows (0) into opportunities; dropped deals table. Renamed interactions to interactions_legacy; created interactions VIEW (UNION ALL of legacy rows + activity_events WHERE verb LIKE 'interaction.%'). Updated promote.ts to write to interactions_legacy (view not directly insertable); all `.from("follow_ups")` callers updated to query tasks with `.eq("type","follow_up")`; contact-activity.ts accepts Task[] for follow-ups arm. Migration files: `supabase/migrations/20260424100000_slice2c_tasks_extend.sql` + `supabase/migrations/20260424110000_slice2c_interactions_view.sql`. All 8 Wave 4 verification gates PASS against live Supabase. pnpm typecheck PASS, pnpm build PASS. BLOCKERS.md: 3 new Open entries (interactions_legacy direct-INSERT writers, interactions_legacy drop deferral, tasks.completed_via_interaction_id audit linkage) all deferred to Slice 3.

- [2026-04-23] **Slice 2B -- Captures consolidation (plumbing session).** Migrated voice_memos, intake_queue, and email_inbox into captures. Extended captures with 5 new columns (source, suggested_target, transcript, metadata, status). Refactored promoteCapture to use adminClient + support 5 explicit promotion targets (task, contact, touchpoint, event, ticket). Added cleanup-audio cron route at /api/captures/cleanup-audio. Migration at supabase/migrations/20260423120000_slice2b_captures_merge.sql ready for manual execution via ~/Desktop/PASTE-INTO-SUPABASE-slice2b.sql. BLOCKERS.md updated with captures-audio lifecycle note. pnpm typecheck PASS, pnpm build PASS.
- [2026-04-23] **Slice 2A -- Spine drop (plumbing session).** Deleted `src/app/api/spine/` (11 route files) and `src/lib/spine/` (queries.ts, parser.ts, types.ts). Deleted 5 spine-only today-page components (tier-alerts, overdue-commitments, today-focus, recent-captures, week-stats). Cleaned `today/page.tsx` (removed fetchTodayPayload prefetch + supabase/user blocks) and `today/today-client.tsx` (removed spine useQuery, TodayPayloadT, all 5 deleted component imports and JSX references, isLoading guard, payload prop passes, dataUpdatedAt block). Cleaned `task-list.tsx` spine-worktree comment. SCHEMA.md updated: all 5 spine tables marked `dropped`. BLOCKERS.md WR-05 resolved. Migration file written at `supabase/migrations/slice-2a-drop-spine.sql` -- Alex executes manually. `pnpm typecheck` PASS, `pnpm build` PASS.
- [2026-04-22] **Session 6 -- Capture downstream promotion (Blocker #4 closed).** Replaced one-line `processed=true` flip at `src/app/api/captures/[id]/process/route.ts` with a real promotion switch. New helper at `src/lib/captures/promote.ts` exports `promoteCapture()` + mapping helpers (`mapKeywordToInteractionType`, `buildTicketTitle`, `defaultFollowUpDueDate`). On process: `interaction` inserts an `interactions` row (type derived from `parsed_payload.intent_keyword`: `called`/`spoke to`→call, `texted`→text, `met with`/`coffee`→meeting, `lunch`→lunch, fallback→note); `note` inserts `interactions` with `type='note'`; `follow_up` inserts a `follow_ups` row with `due_date = created_at + 3 days`, `reason = raw_text`, `created_via = 'capture'`; `ticket` inserts a `material_requests` row (`request_type='design_help'`, `status='draft'`, `source='internal'`, `title` = first 80 chars of `raw_text` collapsed with ellipsis, `notes` = full `raw_text`). Audit trail persisted in `captures.parsed_payload.{promoted_to, promoted_id, promoted_at}`. Guards: `unprocessed` → 400 "No contact or intent to promote"; `interaction`/`note`/`follow_up` with null `parsed_contact_id` → 400 "Needs a contact" (ticket allowed with no contact since `material_requests.contact_id` is nullable); already-processed capture → 409 with the existing `promoted_id`. `/captures` UI reads the new payload fields and renders "Promoted → {label} (view)" linked to `/contacts/<id>` for interactions + follow_ups or `/materials` for tickets; the Process button is replaced with a "Needs contact" or "Nothing to promote" pill when the capture can't promote. `src/lib/types.ts` extended: `CapturePayload` gains `promoted_to` / `promoted_id` / `promoted_at`; new `PromotedTarget` union. `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS (`/captures` 2.39 kB, 117 kB first-load). New Blocker #7 logged for inline contact picker on captures that lack a matched contact (v2 work).
- [2026-04-21] **Session 5 -- Universal Capture Bar v1, text-only, rule-parser, /captures list.** Pinned-bottom bar mounted at `(app)/layout.tsx` (visible on every authenticated route). New `captures` table with RLS (migration `20260421150000_add_captures_table.sql`). Shared rule-based parser at `src/lib/captures/parse.ts` (word-boundary intent + contact match), used by both API route (`src/app/api/captures/route.ts`) and client preview (`src/components/capture-bar.tsx`). Live parse preview line in Space Mono above input, debounced 300ms, infers intent + matched contact as Alex types. `.showcase-card` glass treatment; rotating placeholder (4 strings, 3s, pauses on focus); GAT Red submit pulse (400ms); top-right toast with "view" link to /captures. `Cmd/Ctrl+Enter` submits; `Esc` blurs. Mobile keyboard-aware lift via `env(safe-area-inset-bottom)` + transform on focus. `/captures` list page with empty state, unprocessed-row indicator (small GAT Red dot), and "process" button (currently just flips `processed=true` -- see Blocker #4). Sidebar nav entry added as slot 2 (after Today). `pnpm typecheck` PASS, `pnpm lint` PASS (zero warnings), `pnpm build` PASS (`/captures` 1.93 kB, 116 kB first-load). 4 blockers logged for v2 work: voice/mic capture, downstream promotion, Claude API parser upgrade, capture editing after submit.
- [2026-04-21] Build vs Plumbing Protocol installed in `CLAUDE.md`. `BUILD.md` + `BLOCKERS.md` seeded at repo root.
- [2026-04-21] **Session 2** -- `/agents/[slug]` dynamic route live locally. Middleware public-route bypass extended to `/agents/`. Layout at `src/app/agents/[slug]/layout.tsx` (GAT-wrapped public shell, Kit Screen fonts, dark screen palette). Page at `src/app/agents/[slug]/page.tsx` renders hero + about + gallery-empty-state + contact + referral footer (Option B, agent-forward). Julie hardcoded in `AGENTS` const with temp headshot fallback + `[PLACEHOLDER]` tagline. JSON-LD RealEstateAgent schema + Open Graph metadata wired per SEO minimum. `pnpm typecheck` PASS, `pnpm build` PASS with `/agents/[slug]` prerendered as SSG at `/agents/julie-jarmiolowski`. Three blockers logged (see `BLOCKERS.md`).
- [2026-04-21] **Session 4 part 1** -- Taglines J1/F1/D1 patched into `AGENTS` const in `src/app/agents/[slug]/page.tsx` (Julie: Optima Camelview resident-realtor line; Fiona: 85258 backyard line; Denise: Paradise Valley + Scottsdale discretion line). Julie real headshot resolved and wired at `/public/agents/julie-jarmiolowski.jpg` (800×798 JPEG, 179KB). Blockers #2, #3, #5 closed. `pnpm typecheck` PASS, `pnpm build` PASS, commit + prod deploy + smoke pending on same turn. Remaining in Session 4: Resend announcement email via `re-email-design` three-draft gates, practice-send only.
- [2026-04-21] **Session 3** -- Fiona Bigbee + Denise van den Bossche added to `/agents/[slug]` alongside Julie. Third-agent pick: Denise (tiebreaker: active Organic-Luxe brochure work = live content, not placeholder rows). Migration call: hardcode, batch contacts migration as post-Session-4 plumbing. `AgentRecord` type tightened (phone/website nullable) with conditional render in the contact block; CONTACT.md for both Fiona + Denise lists email only. "About Julie" eyebrow made dynamic (`About {firstName}` + `Work with {firstName}`). Headshots staged to `/public/agents/{fiona-bigbee,denise-van-den-bossche}.jpg` (JPEG 900w q85 via sips). Julie headshot stays on temp fallback (Blocker #3, scoped to Session 4 prep). `pnpm typecheck` PASS, `pnpm build` PASS -- all three routes prerender as SSG. Commit `7fc593e`, deploy `dpl_E8FFAyDYAKUJaqZmAZvvcp8smdHu` aliased to `gat-bos.vercel.app`. Prod smoke: all three routes HTTP 200 with correct `<title>` + `RealEstateAgent` JSON-LD; both new headshot assets resolve at `/agents/*.jpg`. New blockers: #4 (Fiona+Denise phone/website missing from CONTACT.md), #5 (Fiona+Denise taglines pending copy).
